# 《群像》开发进度（按设计方案落地）

> 记录设计 → 实现 → 测试 的落地状态与剩余路线。设计文档：[`设计宪法-角色命运开盒.md`](设计宪法-角色命运开盒.md)（最高定调）、[`大世界沙盘设计方案.md`](大世界沙盘设计方案.md)（后台发动机）、[`事件耦合与跨玩家关联.md`](事件耦合与跨玩家关联.md)、[`PvE威胁系统.md`](PvE威胁系统.md)、[`游戏开发方案GDD.md`](游戏开发方案GDD.md)、[`产品方案PRD.md`](产品方案PRD.md)、[`验证实验设计.md`](验证实验设计.md)。

---

## 一、已落地的确定性结算原语（`backend/internal/engine/`，纯逻辑、可测试）

「决策用 LLM、结算用代码」的地基。全部有测试。

| 包 | 能力 | 对应设计 |
|---|---|---|
| `engine/decision` | 三层决策路由（反射/决断/结算）`Router.Route` + **归因校验**`ValidateAttribution`（「意外但合理」代码强制）+ 门控意外`GateSurprise` | 宪法 §1.5 / §5 |
| `engine/arbitration` | 零和确定性裁决 `Resolve`（胜率∝Score、频率/顺序无关、付费不进、A-Res 加权抽样） | 宪法 §11.3 反 P2W |
| `engine/relevance` | 相关性 `Score`（relative-importance + noisy-OR）、`HopFidelity`、`StopPropagation`、`RouteFor`（命运三档）、`ConsentTierFor`、`MatchScore` | 事件耦合 M1 / 跨玩家 |
| `engine/encounter` | `ContributionScore`、`AllocateLoot`（贡献分赃走 arbitration）、`PenaltyCap/DegradePenalty`（后果分级闸）、`SoloAllowed` | PvE §5/§6/§7 |
| `engine/status.ApplyBatch` | 批量状态写入（每决策 ~15 次 DB 往返 → 读一次/写一次 + 单事务） | §11.2 |
| `engine/narration` | 祖魂语气命运卡渲染 `Beat`（reason-code+效价→基调，前缀框定，按事件打散变体，零 LLM 确定性） | 「玩家是否愿意读编年史」 |

---

## 二、已接入 session / API 的系统

| 系统 | 实现 | 状态 | 测试 |
|---|---|---|---|
| **归因校验接入决策链** | `session/attribution_bridge.go` + `generateUnitDecision` | ✅ 线上**默认强制**：决策前构造快照（人格/压力/记忆/关系），无源归因优雅回退；自动降级护栏（OOC>15% latch 转影子）；`/healthz` 遥测 | 桥接 + DB 集成 |
| **命运相关性 + 命运收件箱** | `session/fate.go`（`SurfaceFateEvent`/`OpenFateInbox`/`ResolveFateDecision`）+ `events.EmitProcessEvent` 流程事件旁路 | ✅ 可用，HTTP 可达 | DB 集成 |
| **双向耦合：死亡→在乎她的人** | `WorldizeDeath` | ✅ **已接入实战死亡路径**（`service.go` 击杀后 best-effort） | DB 集成 |
| **单人 elite 遭遇全链路** | `session/threat.go`（`ResolveEliteEncounter`/`TriggerEliteEncounter`）| ✅ 撞见→反射护栏→combat_roll 多回合→分赃/惩罚→命运收件箱；HTTP 可触发 | DB 集成 |
| **组队野外Boss全链路** | `session/threat.go`（`ResolveFieldBoss`/`TriggerFieldBoss`）| ✅ 多人多回合（Boss 专打残血、危急者反射撤退）→ 按贡献分赃（epic 仲裁归属 + common 比例瓜分 + 败者补偿）/ 失败各自分级惩罚 → 每人命运卡；HTTP 可触发 | DB 集成 |
| **反射层影子（决策三层模型）** | `session/reflex_shadow.go` 接入 `generateUnitDecision` | ✅ 每次 LLM 决策前用 `decision.Router` 算一遍，量化「本可零成本短路、本可省下的 LLM」比例；`/healthz` `reflex_shadow` 遥测；**不改变行为**，为后续真正短路 LLM 铺路 | 单元 |
| **HTTP 端点** | `/healthz`(attribution + reflex_shadow 遥测)、`GET /api/fate/inbox/:unitId`、`POST /api/fate/decisions/:id/resolve`、`POST /api/sessions/:id/units/:unitId/elite-encounter`、`POST /api/sessions/:id/field-boss` | ✅ | httptest |

> 运行测试：`cd backend && go test ./...`；状态字段守门：`go run ./cmd/statuslint ./...`。

---

## 三、剩余路线（按依赖与价值）

### 近期（不依赖大世界重构，可在现有 session 内继续）
- [ ] 命运收件箱接入 **WS 推送 / Snapshot**，让前端实时看到待决策（当前仅 HTTP 拉取）。
- [ ] **回响 Echo**（「因为你上次的选择，所以这次…」）：把玩家 directive/接管动作做成可被 `order_echo` 引用的事件。
- [ ] **反射层从影子转真短路**：观察 `/healthz` `reflex_shadow.skip_rate` 稳定后，让反射层意图直接落地、跳过 LLM（开关灰度）。
- [ ] **威胁刷新调度** + 把 elite 遭遇接入执行主循环（`decision.Router` StrategicFork 触发自治参与）——需谨慎，影子/开关方式。
- [ ] 归因 `redline` 类前因：等离线宪章落地后接入（当前仅 persona/pressure/memory/relation 可解析）。
- [ ] 翻译模板矩阵（`translation_templates`）：把 reason-code×anchor-kind 渲染成祖魂语气 beat。

### 中期（需大世界作用域升级，对应 `大世界沙盘设计方案.md` §8）
- [ ] **拆巨型 `state_json`**（§11.2 最大工程债）→ region 瘦身 + 旁路表 + `events` 加 `world_id/tick`。
- [ ] **World Bus**：`worlds` + `world_id` + `cross_events`（跨玩家不可篡改唯一事实源）。
- [ ] 切 MySQL + region 分片 + 异步 worker 池调度（§8.2）。
- [ ] prompt 缓存（§11.2 最高 ROI 成本项）。

### 跨玩家 / PvE 进阶（依赖 World Bus）
- [ ] 社会客体撮合（`social_objects` + `MatchScore` + `arbitration` 择人）。
- [ ] 七种交互 + `consent_gate` 三档异步同意。
- [ ] 野外Boss / 世界Boss + 组队（party social_object、异步补位、共享池分赃、黑吃黑 `CROSS_BETRAYAL`）。
- [ ] 关系图传播 + 共享历史证据链。

### 产品 / 验证（对应 PRD / 验证实验设计）
- [ ] 先跑 §11.6 的 W0 预实验 + 假门测试（`landing/` 已就绪）+ 人肉后台，再大投入。
- [ ] 单位经济仪表盘、单 MAU 真实 LLM 成本监控。

---

## 四、贯穿的红线（已在代码/设计层守住）
- **付费不改命运胜负**：战斗/分赃/掉率走 `combat_roll`/`arbitration` 确定性、付费不进 Score；受保护字段禁直改（statuslint 守门）。
- **D0–D3 保护**：后果分级闸硬锁不可逆（`encounter.DegradePenalty` + elite 候选层=1，测试验证 lives 永不归零）。
- **留痕不可篡改可仲裁**：一切经 `status.Mutator` + reason-code，或 `EmitProcessEvent` append-only。
- **祖魂身份**：玩家影响处境不改灵魂；抗命经归因叙事化为「她在变成她自己」。
