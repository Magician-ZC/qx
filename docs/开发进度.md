# 《群像》开发进度（按设计方案落地）

> 记录设计 → 实现 → 测试 的落地状态与剩余路线。设计文档：[`设计宪法-角色命运开盒.md`](设计宪法-角色命运开盒.md)（最高定调）、[`大世界沙盘设计方案.md`](大世界沙盘设计方案.md)（后台发动机）、[`事件耦合与跨玩家关联.md`](事件耦合与跨玩家关联.md)、[`PvE威胁系统.md`](PvE威胁系统.md)、[`游戏开发方案GDD.md`](游戏开发方案GDD.md)、[`产品方案PRD.md`](产品方案PRD.md)、[`验证实验设计.md`](验证实验设计.md)。

---

## 一、已落地的确定性结算原语（`backend/internal/engine/`，纯逻辑、可测试）

「决策用 LLM、结算用代码」的地基。全部有测试。

| 包 | 能力 | 对应设计 |
|---|---|---|
| `engine/decision` | 三层决策路由（反射/决断/结算）`Router.Route` + **归因校验**`ValidateAttribution`（「意外但合理」代码强制）+ 门控意外`GateSurprise` | 宪法 §1.5 / §5 |
| `engine/arbitration` | 零和确定性裁决 `Resolve`（胜率∝Score、频率/顺序无关、付费不进、A-Res 加权抽样） | 宪法 §11.3 反 P2W |
| `engine/relevance` | 相关性 `Score`（relative-importance + noisy-OR）、`HopFidelity`、`StopPropagation`、`RouteFor`（命运三档）、`ConsentTierFor`、`MatchScore` | 事件耦合 M1 / 跨玩家 |
| `engine/encounter` | `ContributionScore`、`AllocateLoot`（贡献分赃走 arbitration）、`PenaltyCap/DegradePenalty`（后果分级闸）、`SoloAllowed` | PvE §5/§6/§7 |
| `engine/status.ApplyBatch` | 批量状态写入（每决策 ~15 次 DB 往返 → 读一次/写一次 + 单事务） | §11.2 |
| `engine/narration` | 祖魂语气命运卡渲染 `Beat`（reason-code+效价→基调）+ **`BeatWithAnchor`**（reason×anchor 翻译矩阵：加一句「凭什么牵动她」）| 「玩家是否愿意读编年史」/ 宪法 §4.1 |
| `engine/attachment` | 牵挂等级 `Compute`（100·sigmoid(共鸣+在世+回访+共创)，对各输入单调、**不可付费**、纯函数）；接 encounter 后果分级闸 | 宪法 §4.4 |
| `villageseed` | 确定性「出生即 20 人关系网」`Generate`（FNV+splitmix64，含人格/种子记忆/秘密/人生目标/四轴关系）| 宪法 §4.5 |
| `worldbus` | 跨玩家世界总线 `Append`/`ListByWorld`/`ListForCharacter`（`cross_events` append-only、无 FK、权威排序=谁先动手） | 大世界 §8 / 跨玩家唯一事实源 |
| `world` | 多世界注册表 `Create`/`Get`/`List`/`Seal` + 权威时钟 `AdvanceTick`（单调，cross_events.world_tick 发号器）+ 角色归属 `Join`/`Members`/`WorldOf`（无 FK，幂等） | 大世界 §8 多世界模型根 |
| **M0 双驱动兼容** | mysql/schema 补四表；`AdvanceTick`/`Join`/world boss 扣血的 `RETURNING`/`MAX`/`ON CONFLICT` 按 `dbdialect.IsMySQL` 分支（SQLite RETURNING；MySQL `SELECT…FOR UPDATE`+`UPDATE`/`INSERT IGNORE`）；`RecordCrossInteraction` 改事务原子 | ✅ SQLite 全绿 + **真实 MySQL 8 集成测试**（并发发号单调/幂等接入/Boss 扣血到死） |
| **M1.2 产品埋点** | `internal/analytics`（`product_events` 表 + `Emit`，AARRR 漏斗，append-only 无 FK best-effort）+ `PLAYER_INTERVENTION` reason-code；decision_pending/resolved 接入 fate.go | ✅ 单元测试 |
| **M1.3 prompt 前缀缓存** | 决策 system prompt 改为字节恒等（身份移到 user prompt），约 2800 token 静态块可被供应商前缀缓存命中 | ✅ 缓存不变量守护测试 |
| **M1.1 成本基准** | `cmd/costbench`（真实端点跑 N 次决策，按统一单价表算每次/每角色每日/每月成本；导出 `session.EstimateLLMCostUSD`）| ✅ 编译/无 key 优雅退出；待真实 key 出数 |
| **M1.4 假门落地页** | `landing/`（3 变体 A/B + 调研表 + 26 处埋点）已就绪 | ✅ 早期已建 |

---

## 二、已接入 session / API 的系统

| 系统 | 实现 | 状态 | 测试 |
|---|---|---|---|
| **归因校验接入决策链** | `session/attribution_bridge.go` + `generateUnitDecision` | ✅ 线上**默认强制**：决策前构造快照（人格/压力/记忆/关系），无源归因优雅回退；自动降级护栏（OOC>15% latch 转影子）；`/healthz` 遥测 | 桥接 + DB 集成 |
| **命运相关性 + 命运收件箱** | `session/fate.go`（`SurfaceFateEvent`/`OpenFateInbox`/`ResolveFateDecision`）+ `events.EmitProcessEvent` 流程事件旁路 | ✅ 可用，HTTP 可达 | DB 集成 |
| **双向耦合：死亡→在乎她的人** | `WorldizeDeath` | ✅ **已接入实战死亡路径**（`service.go` 击杀后 best-effort） | DB 集成 |
| **跨玩家耦合：世界总线→她的命运** | `session/cross_link.go`（`SurfaceCrossEventsForCharacter`） | ✅ 别家玩家角色的救援/背叛/结盟从 `worldbus` 流进她的命运收件箱；对手为跨分片角色（非本库 units）也不破 FK；卡面祖魂语气、不泄露原始 ID | DB 集成 |
| **跨玩家交互写入（闭环）** | `session.RecordCrossInteraction`（世界时钟发号→写总线） | ✅ 端到端闭环：建世界→接入→记录交互→桥接进对方收件箱，全程 HTTP 可达 | DB 集成 + httptest |
| **M3 回响 Echo + 玩家动作事件** | `session/echo.go`（`RecordPlayerIntervention`/`loadRecentPlayerActions`/`SurfaceEcho`）+ `PLAYER_INTERVENTION`/`ECHO_LINK` reason-code | ✅ 玩家接管→可引用事件；近期玩家动作 ID 暴露给归因（order_echo 可合法引用）；某次自治选择被归因到真实玩家动作时生成「因为你上次…，这一回…」回响卡；**无真实前因绝不编造**（宪法 §6.2）；HTTP `/intervene` | DB 集成 |
| **M4.1 命运 WS 实时推送** | `Service.Broadcaster`（`*ws.Hub` 满足）；`SurfaceFateEvent`/`SurfaceEcho` 推 `fate_inbox`/`fate_echo` | ✅ 前端命运收件箱免轮询 | 假广播器测试 |
| **M4.2 威胁刷新** | `session/threat_refresh.go`，部署边界 surface-only 确定性刷威胁出没卡 | ✅ 给世界添「活气」不扰动战斗 | DB 集成 |
| **M4.3 连世界+写总线** | `State.WorldID` + `AssignSessionToWorld` + `recordWorldizedKill`（gate 在 WorldID）| ✅ 接入世界后击杀自动成跨玩家事实 | DB 集成 |
| **M5 完整命运前端** | `frontend/src/fate/`（`FateApp` 捏人+离线宪章+即时快照 onboarding、`FateView` 四槽界面）+ `main.tsx` 按 `#fate` hash 路由 + `api.ts` fate 方法/WS 处理 + 后端 `OpenFateFeed`/`GET /api/units/:id` | ✅ 玩家可见的命运开盒 UI：状态卡/高光卡/待决策/回响带 + 托梦接管 + 祖魂语气换皮（家训/托梦/疾呼）；WS 实时；与旧战棋客户端分离 | `npm run build`(tsc+vite) 绿 + 后端 e2e 冒烟 |
| **单人 elite 遭遇全链路** | `session/threat.go`（`ResolveEliteEncounter`/`TriggerEliteEncounter`）| ✅ 撞见→反射护栏→combat_roll 多回合→分赃/惩罚→命运收件箱；HTTP 可触发 | DB 集成 |
| **组队野外Boss全链路** | `session/threat.go`（`ResolveFieldBoss`/`TriggerFieldBoss`）| ✅ 多人多回合（Boss 专打残血、危急者反射撤退）→ 按贡献分赃（epic 仲裁归属 + common 比例瓜分 + 败者补偿）/ 失败各自分级惩罚 → 每人命运卡；HTTP 可触发 | DB 集成 |
| **世界Boss异步协作** | `session/world_boss.go`（`SpawnWorldBoss`/`StrikeWorldBoss`）| ✅ 全服共享血池、异步各自出手→总线即贡献账本→血池清零全员分赃；原子扣血 + 单次结算闩锁防双结算（含并发 race 测试）；HTTP 可触发 | DB 集成 + 并发 |
| **反射层影子（决策三层模型）** | `session/reflex_shadow.go` 接入 `generateUnitDecision` | ✅ 每次 LLM 决策前用 `decision.Router` 算一遍，量化「本可零成本短路、本可省下的 LLM」比例；`/healthz` `reflex_shadow` 遥测；**不改变行为**，为后续真正短路 LLM 铺路 | 单元 |
| **HTTP 端点** | `/healthz`(attribution + reflex_shadow 遥测)、`GET /api/fate/inbox/:unitId`、`POST /api/fate/decisions/:id/resolve`、`POST /api/sessions/:id/units/:unitId/elite-encounter`、`POST /api/sessions/:id/field-boss`、`POST/GET /api/worlds`、`POST /api/worlds/:worldId/join`、`POST /api/worlds/:worldId/interactions` | ✅ | httptest |

> 运行测试：`cd backend && go test ./...`；状态字段守门：`go run ./cmd/statuslint ./...`。

---

## 三、剩余路线（按依赖与价值）

### 近期（不依赖大世界重构，可在现有 session 内继续）
- [ ] 命运收件箱接入 **WS 推送 / Snapshot**，让前端实时看到待决策（当前仅 HTTP 拉取）。
- [ ] **回响 Echo**（「因为你上次的选择，所以这次…」）：把玩家 directive/接管动作做成可被 `order_echo` 引用的事件。
- [x] **反射层从影子转真短路**（降本）：`reflex_shortcircuit` 让日常安静 tick（Router NeedsLLM=false 且 hold/continue）的单位决策由反射层零成本落地、跳过 LLM 含 prompt 构造；安全反射（HP 危急撤退）仍交 LLM。flag `QUNXIANG_REFLEX_SHORTCIRCUIT` 默认关→退化为纯影子统计；`/healthz` `reflex_shadow` 出 `short_circuited`。
- [~] **把 elite 遭遇接入 region-runner 主循环**（`decision.Router` StrategicFork 触发，方案见 [`region-runner-PvE接入方案.md`](region-runner-PvE接入方案.md)）：
  - [x] **PvE-1（shadow）**：region-runner 唤醒**当前 HOT** 单位时先确定性 `rollThreat`（FNV-64a，3% 命中）；命中过 `decision.Router`——HP 危急(<25%)→反射撤退保命(`ActionFlee`)、否则 `StrategicFork`→升级遭遇；威胁短路日常 ambient（消耗本次唤醒）。真遭遇结算走**注入式 `threatHandler`**（保持 regionrunner 不依赖 session），PvE-1 默认 `handler==nil → 只计遥测不改单位`。flag `QUNXIANG_REGION_RUNNER_THREATS` 默认关；`/healthz` 出 `threats_enabled/rolled/encountered/fled/encounter_errors`。新增 `regionrunner/threat.go`。经对抗式评审（3 视角/**0 load-bearing/0 误报**——动力学「饥饿单位饿死/HOT 自旋」顾虑经核验均不成立：3% 命中下饿单位仍 97% 觅食、遭遇后 HOT 重排不自旋）。9 测试含 `rollThreat` 确定性+分布、Router 撤退/遭遇分支、flag/HOT 门控、shadow 不改单位、handler 调用、端到端短路 ambient，均 -race 通过。
  - [x] **PvE-2（真触发）**：main 在 `QUNXIANG_REGION_RUNNER_THREATS_APPLY`（默认关）注入 `session.TriggerEliteEncounter` 包装（region-runner 专用长生命 `Service`，保持 regionrunner 不依赖 session），命中威胁时真跑 elite 遭遇（多回合 `combat_roll` + Mutator 改 HP、分赃/惩罚、命运卡，LLM-free）。端到端测试（regionrunner 包内引 session，`firstTick`+`withClock` 确定性命中）证 HOT 单位被唤醒→真遭遇→受伤 HP<100。经对抗式评审（2 视角/**0 load-bearing**，1 误报已驳）；据评审内核补文档：威胁 roll 依赖 `APPLY=true`（在 `applyAmbientL1` 内），完整真打需 `ENABLED+APPLY+THREATS+THREATS_APPLY` 四开。**并发前置（已登记 PvE-3）**：`ResolveEliteEncounter` 用 `Mutator.Apply`（非 Optimistic），`execGuard` 只覆盖异步战斗、不覆盖部署期 HTTP 写 → 战斗可达会话开 `THREATS_APPLY` 前须先落乐观并发硬化（对齐 real-2→real-3-0）。
  - [x] **PvE-3 并发硬化**：`ResolveEliteEncounter` 的每回合 HP / 战利品 Wallet / 失败 Morale 三处写改用 `applyEliteMutation`（`status.Mutator.ApplyOptimistic` + 冲突重读重试，delta 重算正确叠加并发写而非整块覆盖），覆盖**所有并发写者**（不止异步战斗，还有 `execGuard` 不覆盖的部署期 HTTP 写）。单线程（HTTP 手动触发）首次即成功、行为同 `Apply`；field_boss 路径（HTTP-only）共用的 `applyDefeatPenalty` 随之乐观、无并发故等价。并发不变量测试（A 战斗写 hunger×40 / B 乐观 elite 遭遇并发）证「战斗写永不被遭遇覆盖」，且**经退回非乐观验证测试有牙**（非乐观时 hunger=61≠60 失败）、-race×5 稳定。**至此战斗可达会话开 `THREATS_APPLY` 已安全**（对齐 real-2→real-3-0）。
  - [x] **PvE-4 锚加权威胁刷新（威胁天然扎堆她在乎的地方）**：把固定 3% 概率换成 `threat_spawn_score` 加权——`session.AnchorDensity`（`buildRelevanceAnchors` 持久锚+实时关系锚 → `Σ weight·relevance.RelativeImportance` → 饱和归一 [0,1]）经注入式 provider 喂 region-runner；`threatRoll1000`（确定性均匀抽样）< `threatSpawnPerMille(density)`（region 基线项 + 锚项·60‰，夹 [破圈下限 5‰, 上限 80‰]）→ 零锚 ≈1%（破圈：世界仍有危险）、满锚 ≈7%（扎堆她在乎处）。**优化**：仅 draw∈[floor,max) 才查锚密度（省 ~92% DB 查询），经 4000 tick×3 密度的**等价性测试**证与朴素判定逐结果一致。flag 沿用 `QUNXIANG_REGION_RUNNER_THREATS`（shadow 也加权）。经对抗式评审（3 视角/**0 load-bearing/1 误报已驳**）；核心 `AnchorClustering` 测试（同 tick 高锚密度撞威胁/零锚不撞）证「威胁扎堆在乎多的人身上」。
  - [ ] **完整威胁刷新剩余**（后续）：`region.threat_level` 随后台世界事件累积（+ 上限/冷却）取代 MVP 常量；`freshness` 反扎堆项（记录上次遭遇 tick）；`arbitration.Resolve` 区域级选址；field_boss/dungeon 分段异步推进。
- [ ] 归因 `redline` 类前因：等离线宪章落地后接入（当前仅 persona/pressure/memory/relation 可解析）。
- [ ] 翻译模板矩阵（`translation_templates`）：把 reason-code×anchor-kind 渲染成祖魂语气 beat。

### 中期（需大世界作用域升级，对应 `大世界沙盘设计方案.md` §8）
- [x] **M6.1 events 双键**：`dbmigrate` 幂等加列 + events 加 `world_id/region_id/tick`（加列不改义，双驱动）。
- [x] **拆巨型 `state_json`**（§11.2 最大工程债）✅ 完成：影子双写→cutover 两步纪律，逐片拆。**实测满载会话热行 2248.6 KB → 1.2 KB（瘦身 99.9%）**——三大数组全部外移到旁路表，热行不再随历史无界膨胀。
  - [x] **第一片 `decision_traces`**：旁路表 + 影子双写（step1）→ **读写路径 cutover**（step2：load 从表 hydrate、Save 确认写表才从 blob 摘除，经 17 智能体对抗评审加固 8 项，修了持久性「轨迹永活于 {表,blob}∖∅」+ 定宽纳秒排序）。
  - [x] **第二片 `LLMInteractions`**：①**影子双写**（新增 `llm_interactions` 表；因 `appendLLMInteraction` 有 ~25 调用点，改在 Repository.Save 于压缩抹除旧 prompt **之前**持久化全量完整 prompt，跨 Save INSERT-OR-IGNORE 累积；经 15 智能体评审修 2 load-bearing：擦除/异步执行竞态用 `isAsyncExecutionRunning` 在飞拒绝、对称清 `decision_traces`+补 Purge 孤儿）。②**读路径 cutover**：load 经 `hydrateLLMInteractions` 从表读回+merge blob 残留+复用 `compactLLMInteractions` 收敛为与切表前**逐字节一致**的工作集（表保全量完整 prompt 供审计、普通客户端 `publicLLMInteractions` 本就脱敏）；Save 确认写表才从 blob 摘除（持久性「永活于 {表,blob}∖∅」+ hydrate 自愈）。经 16 智能体评审（12 发现/1 真 load-bearing/1 误报），修 **`GetAuditBundle` 读未 hydrate 的裸 Get → 审计 LLM 交互恒空**（改走 `loadSession`，并自动兼容后续 RawEventLog 切表）。
  - [x] **第三片 `RawEventLog`**：新增 `raw_event_log` 表 + `raw_event_store.go`，影子双写+cutover 一步到位。因 `appendRawEvent` 在 append 时即限/裁 payload、裁到 `maxRawEventHistory`，与 blob 同口径（无 LLM 的有损压缩问题），故 **Save 在压缩之后持久化压缩态**（避开 `limitTextRunes` 边界空格非幂等）、hydrate cap-only。GetAuditBundle 已走 loadSession 故自动兼容。经 11 智能体评审（8 发现/**0 load-bearing**/0 误报，消费方轴与隐私红线均验证无回归），按建议把 raw persist 移到 compact 后修了「表与 blob 逐字节一致」的边界空格瑕疵。
  - **✅ state_json 三大增长数组（decision_traces / llm_interactions / raw_event_log）已全部外移到旁路表，热行不再随历史无界膨胀。**
- [x] **World Bus 地基**：`cross_events` append-only 表 + `internal/worldbus`（`Append`/`ListByWorld`/`ListForCharacter`）。跨用户/跨分片角色**刻意无 FK**，权威排序=(world_tick, occurred_at, id) 即「谁先动手」，只追加永不改写。
- [x] **总线→命运收件箱桥接**：`session.SurfaceCrossEventsForCharacter` 把牵涉她的跨玩家事件翻成她的命运卡入收件箱。
- [x] **多世界注册表 + 权威时钟**：`internal/world`（`Create`/`AdvanceTick`/`Join`/`WorldOf`…），`worlds` + `world_members` 表。
- [x] **跨玩家交互写入闭环 + HTTP**：`RecordCrossInteraction`（时钟发号→写总线）+ `/api/worlds*` 端点。建世界→接入→记录交互→桥接进对方收件箱全打通。（剩余：把**实战内**的跨玩家交互自动写总线、跨用户角色撮合、定时拉取桥接。）
- **M7 阶段-2 地基（为 region 调度铺路，§8.7 加列/登记，additive 低风险）**：
  - [x] **M7.1 units 作用域/生命态列**：`units` 加 `world_id/region_id/life_state/last_active_tick`（schema.sql + `dbmigrate.UnitScopeColumns` 双管，fresh/existing 双驱动）。`life_state` 是 `status_json.LifeState` 的去规范化只读索引（Save 单向从 blob 覆盖写，权威仍是 blob）；`world_id/region_id/last_active_tick` 由调度层独占（Save 不写、不覆盖）。新增 `unit/scheduling.go`：`SetUnitScope/SetLifeState/TouchLastActiveTick/ListActiveByRegion/CountActiveByRegion`（unit 包首套测试）。经 12 智能体评审（9 发现/1 真 load-bearing/0 误报），修了 **`SetLifeState` 列内翻转被下次 Save 还原的误导性注释**（澄清单向同步契约 + 加 revert-on-Save 契约测试）。
  - [x] **M7.2 大世界生命周期 reason-codes**：`engine/events` 登记 `CategoryLifecycle` + 7 码（`CHARACTER_BORN/CHARACTER_DIED/VENGEANCE_FULFILLED/FACTION_COLLAPSE/PERSONALITY_DRIFT/LOYALTY_GAIN/LOYALTY_STRAIN`；`PENDING_DECISION/DECISION_RESOLVED` 已存在故未重复）。`status.Mutator` 经 `events.Lookup` 校验，新码即刻可经 Mutator 留痕（events 包首套测试守目录完整性）。
  - [x] **M7.3 调度地基（shadow/additive，未接执行主循环）**：①`internal/engine/scheduler` 纯逻辑原语——`ClassifyTier`（HOT/WARM/COLD 按空闲 tick）+ `NextWakeTick`（节奏 HOT+1/WARM+4/COLD+16，严格向前）+ `ShouldWake` + `AdmitDecision`（在途背压，§9 全局上限 64）。②`internal/agentqueue` 持久化队列——`agent_wake_queue`（按 unit_id upsert / 按 region+到点拉取）+ `agent_decision_jobs`（worker 池 **原子认领** pending→running，双驱动对齐 `world.AdvanceTick`，含 8-goroutine 抢 40 作业无双认领的 race 测试）。③双驱动 schema 两表。经 15 智能体评审（12 发现/3 真 load-bearing/2 误报），修了 **`CompleteJob` 缺 running 态守门**（加 `status=running` 谓词 + RowsAffected 校验，对齐 `world.Seal`）；另两条「接入前置项」（purge/erase 须纳入两表、stale-running 回收+attempt、多世界 region 作用域）已登记进 `agentqueue` 包头作为 **M7.3-real DoD**。
  - [~] **M7.3-real 真接入**（按 [`M7.3-real-region-runner技术方案.md`](M7.3-real-region-runner技术方案.md) 分步推进）：
    - [x] **real-0 前置项（纯加性、不接循环）**：`agentqueue.ReclaimStaleJobs`（stale running→pending+attempt 自增/超限 failed，防 worker 崩溃孤儿抬高背压致饿死）+ 定宽时间戳（claimed_at/created_at 字典序=时间序）+ claimed_at 索引；`ListDueWakes` 加 world_id 维度；**两队列表加 `session_id` 列**（schema + dbmigrate 双管）+ `PurgeExpiredSessionData` 按 session_id 清理两表（+ 计数）。经 9 智能体评审（1 真 load-bearing：purge 原按 region_id 依赖未接入的 region==session 约定、易留孤儿 → 改 session_id 解耦 + 真实 enqueue→purge round-trip 契约测试）。
    - [x] **real-1 RegionRunner 骨架（默认关闭、纯 shadow）**：新增 `internal/regionrunner`——`Runner` 注入式时钟、`schedulePass`（DistinctWakeRegions→ListDueWakes→背压闸→**原子 PromoteWakeToJob 出队**）、`processOne`（worker 认领→**shadow 只记日志不碰单位**→WARM 重排→完成）、`Run`（调度 ticker + N worker[panic 恢复] + 定期 reclaim，ctx 取消优雅 WaitGroup 退出）+ 原子遥测 `Stats`。`main.go` 按 `QUNXIANG_REGION_RUNNER_ENABLED`（默认关）启动、`/healthz` 出 `region_runner` 块。端到端冒烟通过（启用→遥测出、SIGTERM 优雅停）。经 21 智能体评审（18 发现/**0 真缺陷**：2 load-bearing 均为「无死锁无泄漏、零现网影响」正面确认；按建议把 RemoveWake+EnqueueJob 改 **原子 PromoteWakeToJob**、删死代码 timer、登记 RegionID 必填不变量）。
    - [x] **real-2 灰度 L1 决策应用（默认 shadow，`QUNXIANG_REGION_RUNNER_APPLY` 灰度开）**：worker 真应用 L1 觅食/休息生存循环——饿(<40)则 `AMBIENT_FORAGE` 补口粮(+30)经 Mutator + HOT 盯到吃饱；否则 `AMBIENT_REST` 缓慢消耗(-3) + 按 `last_active_tick` 真实冷热分层降温至 COLD。让位战斗（`IsExecutionRunning`/`InCombat`，改前再查一次收窄竞态）；逝者永久 drop、暂态(恢复中)COLD 回查、删档退出。经 15 智能体评审（12 发现/**2 真 load-bearing 均已修**）：①`life_state!=active` 原永久 drop → 改为「仅 dead drop、暂态 COLD 重排」（否则恢复后永久脱离模拟）；②Mutator 读改写无单位级锁、与战斗/HTTP 并发丢更新（pre-existing，runner 暴露）→ 让位+改前复查收窄窗口、`Apply` 默认关兜底，**文件头登记「战斗可达会话开 Apply 前须落单位级串行化/乐观并发」为 real-3 硬化前置**。
    - [x] **real-3-0 单位级并发硬化（乐观并发）**：units 加 `version` 单调列（schema+dbmigrate）；`Repository.Save` 每次 `version+1`（所有写者透明）；新增 `SaveOptimistic`（`WHERE version=读到的值` 条件写）+ `Mutator.ApplyOptimistic`（冲突返回 `ErrConcurrentModification`、不落事件）；region-runner 改用 `ApplyOptimistic`，冲突即退避(conflicted++)+COLD 重排，**绝不覆盖战斗/HTTP 对同一单位的写**。经 12 智能体评审（9 发现/**0 真功能缺陷**：乐观正确性经核验通过 + 并发不变量测试 `-race×10` 证「A 战斗扣 HP×60 不被 B 离线觅食覆盖、最终 HP==40」；唯一 load-bearing 是 `SaveOptimistic` 注释把「写哪些列」说反——已订正为如实陈述+「安全来自 version 守护而非没写 Social」）。**至此战斗可达会话开 Apply 已安全。**
    - [x] **real-4a 更丰富的环境动作空间**：离线动作从 {觅食,休息} 扩到 {觅食,休息,**社交**,**反思**}，为 real-3 HOT-LLM 备好「值得用 LLM 决」的选择空间。新增 `AMBIENT_SOCIALIZE/AMBIENT_REFLECT` reason-code（CategoryEmotion·morale 域）；把 `applyAmbientL1` 重构成 `decideAmbientReflex`（零 LLM 反射：饿→觅食/士气低→社交/心满意足→反思/否则休息）→ `applyAction`（经 `ApplyOptimistic` 落地）→ `countAction`；主动动作(觅食/社交)→HOT、被动(休息/反思)→自然降温。经对抗式评审（3 视角/**2 真 load-bearing 均已修**）：①**reflect 发散吸引子**——满意单位(morale>0.8)单向爬到 clamp 上界 1.0 后每 COLD 周期永久空写 `AMBIENT_REFLECT`（污染 32 槽记忆环、灌垃圾 events 行、reflected 遥测虚增、version churn）→ 加 **`ambientSaturated` 饱和空写短路**（动作把已达界字段继续往界外推时跳过落地/计数/升 HOT，仅 `settled++` 自然降温；通用形防未来动作重蹈「触发带⊂效果方向」），补多步收敛测试（reflected 有界=5、settled 接管）；②**乐观冲突路径零测试**（applyAction 仅 50% 覆盖）→ 补并发不变量测试（战斗 HP 写永不被离线整块回写覆盖）+ 未知动作/边界/精确增益测试。〔记：morale 离线只升不降是单向棘轮，real-3/4b 前需定夺是否加离线 morale 衰减——评审已标为有意设计决策，非疏漏。〕
    - [x] **real-4b 单位 seed/接入世界（让循环真有单位跑）**：新增 `session/ambient_scheduling.go`——`seedAmbientForUnits`（为单位登记作用域 region=sessionID/生命态列 active/入初始唤醒 WakeAtTick=0·COLD；best-effort、幂等、开关关 no-op）+ `seedAmbientForNewUnit`（中途造人单点策略：仅玩家阵营才进调度）。接入**所有 live 玩家造人点**：建局 `createSinglePlayerWithMapScript`（非 draft）、组队 `ApplyOpeningDraft`（单人局始终走此）、**中途婚育子嗣** `resolveDuePregnancies`、**野民归化** `maybeConvertWildling`。gating 经 `deps.RegionRunnerEnabled`→`Service.SetAmbientSchedulingEnabled`（per-request 注入，默认关→默认建局链路零成本、也不在运行器开启时撞历史脏唤醒）。测试：契约（写出 wake/scope/lifestate 恰为 runner 输入）、disabled-noop、幂等、阵营门控、**真端到端**（seed→启真 `regionrunner.Run`→饿单位被唤醒觅食，0.28s 完成）。经对抗式评审（3 视角/**1 真 load-bearing 已修**）：中途造人（婚育/归化）漏 seed → 中途出生的玩家单位永久缺席离线生活（runner 只认 wake 队列、无初始 wake 即永不进循环也永不自重排）→ 把 4 处造人点策略集中进 `seedAmbientForNewUnit` 并补阵营门控回归测试。
    - [x] **real-3 HOT 单位上 LLM 离线决策**：新增 `regionrunner/ambient_llm.go`——**当前 HOT（正活跃）**的单位升级到 `ai.GenerateJSON` 在 {觅食/休息/社交/反思} 间选动作（Task=downtime，schema 限 enum 四动作，MaxTokens 80）；非 HOT / 未启用 / 预算耗尽 / LLM 任何失败（错误/解析/越界） → 一律回退反射 `decideAmbientReflex`，LLM 动作仍照常过饱和短路 + `ApplyOptimistic` 落地。整块 flag-gated（默认 `llm==nil` 全程反射；main 按 `QUNXIANG_REGION_RUNNER_LLM` 注入才启用）。离线版机制（runner 无 session State）：**进程级成本闸**（注入式成本函数沿用 `session.EstimateLLMCostUSD` 同一单价表，micro-USD atomic 累计，超 `QUNXIANG_REGION_LLM_BUDGET_USD` 即 latch 转反射）；**有意不接归因**（ambient 四动作平淡无 OOC 风险，注明动作空间扩到戏剧性选择时须补离线归因）；`/healthz` 出 `llm_enabled/llm_calls/llm_fallbacks/llm_spent_usd/llm_latched`。经对抗式评审（4 视角/**2 真 load-bearing 均已修**）：①**预算闸 fail-open**——`budgetUSD<=0`（配置笔误 0/负，`envFloatDefault` 只在解析错误时回退故 `=0` 被原样接受）会让 0 当「不限」、烧钱护栏静默失效 → money guardrail 改 **fail-safe**：`SetLLMClient` 把 `<=0` 夹到正默认 + `llmBudgetAllows` 的 `<=0` 翻成禁用 + main 侧也拦非正值；②**45s 超时只管单次 HTTP 不管整个决策**——`ai.Service` 跨 provider×endpoint 故障链每段各拿一份 `req.Timeout`、无整体上限，LLM 退化时单次决策可累积数分钟长占 worker → 用**派生 ctx 给整个决策套 45s 硬上限**（单次 HTTP 用更小的 20s），Run 取消即时生效。测试：HOT 门控、LLM 动作端到端落地（区别于反射）、四类回退各一、预算 latch、非正预算夹紧不退化成无上限、整体 deadline 套用。〔遗留：离线 morale 单向棘轮待 playtest 数据定夺是否加衰减；ambient LLM prompt/response 暂不持久化（结果仍经 Mutator 留痕）。〕
    - [x] **real-5 MySQL 驱动验证**：起真 MySQL 8.0 容器跑全栈集成验证（DSN 门控测试 `regionrunner/mysql_integration_test.go`，无 `QUNXIANG_MYSQL_DSN` 自动 skip 故不扰常规 `go test ./...`），覆盖三条 MySQL 专有方言路径——①唤醒队列 `ON DUPLICATE KEY` upsert、②`ClaimNextJob` 的「内部事务 SELECT…FOR UPDATE + UPDATE」并发认领（8 worker 抢 40 作业无双认领）、③unit `Save` 的 `ON DUPLICATE KEY` + `SaveOptimistic` 的 `version` 守护条件写（并发不变量：战斗 HP 写永不被离线觅食覆盖）+ schedulePass→processOne→觅食全链路。**实证抓出并修复 1 真 load-bearing**：MySQL `applySchema` 的「整块 TrimSpace 后以 `--` 开头即跳过」逻辑，把「注释行 + `CREATE TABLE agent_wake_queue`」同一 `;`-块整体吞掉 → **该表在 MySQL 部署下从未建表、region-runner 唤醒队列彻底失效**（SQLite 的 applySchema 不跳 `--` 块故不受影响）。修为**逐行剥离 `--` 注释行再判空**（`stripSQLLineComments`）：纯注释块变空被跳、注释+语句块保留语句。全新空库从零建全 + 残留半库再 Open **自愈补建**（CREATE TABLE IF NOT EXISTS 每次 Open 重跑）均 -race 通过。
- [ ] region 分片 + 异步 worker 池水平扩展（§8.2，real-5 已验证单实例 MySQL 正确性）。
- [x] **prompt 缓存（§11.2 最高 ROI 成本项）**：`ai/prompt_cache.go` 进程内 TTL+LRU，相同情境（task+schema+完整 prompt，不含身份 Metadata）的成功决策在 TTL 内复用、跳过 LLM；只缓存真 LLM 成功输出（fallback 不入），命中 Output 仍下游 re-validate 故安全。flag `QUNXIANG_PROMPT_CACHE` 默认关，统计出 `/healthz` `ai.prompt_cache`。

### 跨玩家 / PvE 进阶（依赖 World Bus）
- [x] **社会客体撮合**：`internal/socialobject`（social_objects+members 双驱动存储）+ `session.MatchIntoSocialObject`（四因子 `relevance.MatchScore`→`PassesMatch` 门→`arbitration.Resolve` 确定性择 slots 人，胜率∝Score/频率无关/付费不进→绑成员+`SOCIAL_OBJECT_BIND` 留痕）；四因子由调用方按情境算，原语通用。HTTP `POST/GET /api/worlds/:worldId/social-objects`。
- [x] **七种交互 + `consent_gate` 三档异步同意**：`session/seven_interactions.go`（结识/结盟/交易/联姻/反目/复仇/开战→worldbus 类型+后果层+四轴关系增量；`RecordSevenInteraction` 先记世界总线再按 `ConsentTierFor` 路由：单方立即应用/高后果建待决同意）+ `consent_gate.go`（consent_requests 表，Resolve 原子守门防双 accept，accept 应用/reject 不应用/expire 兜底，D1「能听见不能强迫」）。worldbus 补 3 新 kind。HTTP seven-interactions/consent 端点。
- [x] 野外Boss（组队）+ 世界Boss（异步共享血池）+ 贡献分赃。世界Boss经多智能体对抗式评审加固：扣血+记账同事务、账本读取去上限、贡献按单次最高伤害（频率无关反 P2W）、spawn 校验世界存在/血量上限。（剩余：party social_object 撮合、黑吃黑 `CROSS_BETRAYAL`、结算与闩锁同事务、strike 鉴权/行动力。）
- [ ] 关系图传播 + 共享历史证据链。

### 产品 / 验证（对应 PRD / 验证实验设计）
- [~] **W0 预实验 + 假门测试**（后端支撑已落地）：`fake_door_leads` 表（双驱动）+ `POST /api/leads`（landing 留资/问卷/事件 append-only 落库）+ `GET /api/ops/leads-funnel`（按 kind 计数+唯一访客）；`landing/index.html` formEndpoint 指向 `/api/leads`。剩余：人肉后台运营 + 真实投放跑实验。
- [x] **单位经济仪表盘 + 单 MAU LLM 成本监控**：`GET /api/ops/cost-dashboard?days=N` 跨会话聚合真实 LLM 成本（沿用 estimateLLMCostUSD 口径，按 provider 拆分/总 token/fallback 率/活跃会话数 MAU 代理）+ 单位经济（按生命态计数）。MVP 流式解析 llm_interactions JSON；扁平成本列/钱包 SUM 是规模化后续。

---

## 四、贯穿的红线（已在代码/设计层守住）
- **付费不改命运胜负**：战斗/分赃/掉率走 `combat_roll`/`arbitration` 确定性、付费不进 Score；受保护字段禁直改（statuslint 守门）。
- **D0–D3 保护**：后果分级闸硬锁不可逆（`encounter.DegradePenalty` + elite 候选层=1，测试验证 lives 永不归零）。
- **留痕不可篡改可仲裁**：一切经 `status.Mutator` + reason-code，或 `EmitProcessEvent` append-only。
- **祖魂身份**：玩家影响处境不改灵魂；抗命经归因叙事化为「她在变成她自己」。
