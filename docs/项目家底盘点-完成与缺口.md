# 群像战棋 · 项目家底盘点（已完成 / 缺口）· 更新版

> 生成方式：多智能体**逐项对照设计文档与实际代码核验**（非照抄进度/提交信息），每条附 `file:line` 或文档章节证据。
> 本版在前一版基础上，对本会话改动的 8 个子系统做了**重新核验**（命运前台 / combat_shake / 引擎原语接入度 / 治理安全 / 大世界分片 / 跨玩家PvE / 商业化 / 前端覆盖度），未改动子系统沿用上轮结论并标注增量。
> 状态口径：**[x] done**＝设计承诺已完整落地且接入可用；**[~] partial**＝部分落地 / 有能力但未接主链路 / 默认 flag 关；**[ ] missing**＝仅文档或仅 stub / 死代码。
> 关键判据：本项目最重要的成熟度区别是「**原语/能力已实现**」≠「**已接入玩家可感知主链路**」。

---

## ⚠️ 复核修订（2026-06-08，基线 git HEAD=68cbfd4）

> 本块为后一轮 **25 智能体深度交叉验证**（12 域并行回代码实证 + 对抗复核 + 工程门禁实跑）的修订结论，**优先级高于下方旧正文**。完整证据见 [`审计复核-文档vs代码-2026-06-08.md`](审计复核-文档vs代码-2026-06-08.md)。
>
> **核心更正：本盘点旧版显著低估了完成度。** 下方「五、部分完成」「六、完全没做」里有 **15 条"缺口"实际已由 commit `79dc17f`（三道门）/`68cbfd4`（补四缺口）端到端修复**，盘点写于其修复未完全反映之时。逐条已重新核实落地（带 `file:line`）：

**已修复、需从「缺口」移出（多为 flag 默认关，代码就绪）：**
- ✅ **账户级成本闭环「三连缺」全通**：`session` 已绑 `account_id`（`types.go:532`）；`billing.AddSpend` 24 个非测试调用点记账；`CheckQuota` 经 `accountOverQuota→llmBlocked` 接进 7 个 LLM 站点前置拦截；`ListEntitlements` 有 `GET /api/billing/entitlements/:accountId` + 前端 `BillingPanel`。（旧 §六"完全没做/死代码"作废。`chargeLLMQuota` 确为死代码但职责已被 `SpendRecorder` 取代）
- ✅ **合规硬门槛已强制**：`complianceGate()` 中间件挂建局/双人局/advance-phase 3 chokepoint（`router.go:509/560/1128`）；`RecordPlaySeconds` 已 2 处调用累加。（旧 §五/六"未成年/防沉迷未强制=没门"作废）
- ✅ **前端 UI 已全覆盖并挂载 `App.tsx`**：治理 `GovernancePanel`、商业化 `BillingPanel`、实名 `ComplianceGate`、跨玩家 `ConsentInbox` 全部实现 + api wrapper 齐。（旧 §六"治理/商业化/跨玩家 UI 全缺/零界面"作废；唯 **ops 运营看板前端**仍缺）
- ✅ **命运卡倒计时前端已渲染**：`FateCountdownBar` 每秒重渲标红/标灰（`FatePanel.tsx:176`/`FateView.tsx:271`）。（旧 §五"倒计时前端不渲染"作废）
- ✅ **`status.Mutator.ApplyBatch` 已接 8 处生产路径**（`68cbfd4`，`applyCombatGroupDynamics` 在战斗主循环调用）。（旧 §六"零非测试调用方/死代码"作废）
- ✅ **region 注册表已有包外调用方 + region_leases 已接 runner 主循环**（`regionrunner.go` 多处）。（旧 §六"无任何包外调用方"、旧矛盾口径作废）
- ✅ **`SurfaceCrossEventsForCharacter` 已接部署边界 live 调用**（`cross_link.go:29→service.go:843`）。（旧 §五"无 live 调用方"作废）
- ✅ **漏斗 12/13 事件有真实 Emit**（唯 `return_visit` 刻意走 `/api/leads` 防轮询灌爆）。（旧"5/7 无 Emit"作废）
- ✅ **前端测试真跑真过**：vitest 20 用例（含抗命卡解析 12 断言 + 倒计时 8）。（旧 §六"写了不跑"作废）
- ✅ **工程门禁**：`go vet` 净（trade.go unreachable 不复现）；statuslint 仅剩 `romance.go:1001` 1 处（旧记 3 处）。

**新瓶颈（取代旧"没做"叙事）：项目真实瓶颈已从「没做」转移到「做了但 flag 默认关 → 默认部署玩家零感知」。** flag 清单：`QUNXIANG_BILLING_ENABLED`（关→4 路由不注册）、`QUNXIANG_COMPLIANCE_ENABLED`（关→**端点整组 404** + Gate 恒放行）、`QUNXIANG_CONTENT_SAFETY`（关→chokepoint 恒放行）、`QUNXIANG_MAIN_VILLAGE`、`QUNXIANG_BLOOD_FEUD`、`QUNXIANG_REFLEX_SHORTCIRCUIT`、`QUNXIANG_PROMPT_CACHE`、`QUNXIANG_REGION_RUNNER_*`、`QUNXIANG_IAP_REAL`。

**仍真实未做（partial/missing，下方正文仍有效的部分）：** 命运前台乐趣钩子（情境化 Copilot/会话收尾卡/可分享死亡传记卡/命运卡 `AttributionZH` 恒空死管线）；离线宪章 `offline_charter`（`Directive` 无 `Scope`）→ redline 接归因 → Freeze List/reassessGoal/六维野心/人格漂移；未成年模式内容分级（`minor_mode` 零读取方）；world boss/七交互/撮合无玩家 UI 无自治驱动；blood_feud 多跳传播（**唯一真 missing**，只做 hop=0）；多实例 leader 选举/稳定哈希；分 tier/前缀缓存；2 个 WS 通道 bug；前端 lint 6 errors。

> **高危**：🔴 IAP stub 刷单（billing 开但 IAP_REAL 关 → 任意收据恒发权益）；🟠 未成年模式 `minor_mode` 零读取方（年龄分级未生效）。

---

## 一、一句话总评

**项目仍是「机制扎实、原语精良的 First Playable 原型」，但本会话把「命运开盒前台、战斗情绪深度、可靠性、安全合规与商业化地基」从缺口推进到了可用——核心乐趣闭环首次接进主客户端。**

相比上一版的最大变化：命运四槽 + 抗命溯源卡 + PvE 入口**已接进主指挥客户端**（不再只活在独立 `#fate`）；combat_shake 高压情绪覆盖**从死代码变为主循环实接**；技术债清理使 `go vet` / statuslint 转净（3→1）；内容安全、ops 端点鉴权、真实 IAP/实名网关、region 分片、商业化 billing 全部落地（多为 flag-gated 切片）。**仍未跨过的三道门**：① 后端能力的**前端 UI 覆盖**（治理/商业化/跨玩家仍无界面）；② **自治驱动**（PvE/跨玩家/撮合仍靠 ops-HTTP 手动触发，非玩法自动产生）；③ **账户级成本闭环**（配额闸是死代码，根因是 session 未绑 account_id）。

---

## 二、成熟度总览（★=本会话有实质推进）

| 子系统 | 成熟度 | 一句话 |
|---|---|---|
| 核心回合循环 | ■ 基本完成 ★ | 最成熟；本会话补了 ATB/combat_roll/记忆衰减确定性单测 |
| 战斗系统与 combat_shake | ■ 基本完成 ★ | combat_shake 从死代码变主循环实接（修 retreat/rage 两 load-bearing） |
| 命运层与命运开盒前台 | ■ 基本完成 ★ | M2/M3 硬规则 + 四槽/抗命卡接主客户端；缺倒计时前端、主局村庄 |
| AI-LLM 子系统 | ■ 基本完成 | 单机编排成体系；账户级护栏/分tier/前缀缓存/批量决策仍未做 |
| 社交关系外交服从 | ■ 基本完成 | 落地最全之一；自主社交链死代码、redline 未接 |
| 生存经济生产 | ■ 基本完成 ★ | 七大块全接主链路；本会话清了 trade 死码 + hunger 走 Mutator |
| 记忆叙事 | ■ 基本完成 ★ | 机制层最全；本会话补衰减单测；传记卡/编年史/记忆种子仍缺 |
| 治理安全合规 | ◧ 部分完成 ★ | 内容安全/ops鉴权/实名网关已落；未成年门未强制、账户配额死代码 |
| 持久化与大世界沙盘 | ◧ 部分完成 ★ | region 分片接主循环 + regions/world_ticks 表；注册表无调用方 |
| 实时通道与前端覆盖度 | ◧ 部分完成 ★ | 命运/抗命/PvE 接主客户端；治理/商业化/跨玩家 UI 全缺 |
| 引擎原语层接入度 | ◧ 部分完成 | attribution/relevance/arbitration 接了；Router/ApplyBatch/scheduler 仍未接默认链路 |
| 跨玩家与 PvE | ◧ 部分完成 ★ | elite 接主客户端；field/world boss + 七交互 + consent 仍 ops-only、无自治驱动 |
| 产品验证与商业化 | ◧ 部分完成 ★ | billing+真实IAP+漏斗埋点落地；账户配额闸死代码、商业化前端零覆盖 |

---

## 三、本会话相比上一版盘点的「净推进」

- 命运四槽（FatePanel）+ 抗命溯源卡（DefianceCard）**接进主指挥客户端 App.tsx**（顶栏「命运」按钮、活动流/决策摘要三处渲染抗命卡，防机器编码裸泄）——核心卖点首次对主战棋玩家可感知。
- combat_shake 高压情绪覆盖**接入决策主循环**（resolveExecution 调 resolveCombatShake，三路 Modifiers 乘性合流），并修 retreat（返回相邻格）/rage（用可见敌集）两处 load-bearing。
- 命运 **M2/M3 硬规则**：每日待决策≤3 防轰炸、48h 过期倒计时、过期兜底回响卡、后果分级闸（牵挂放大 urge 代价、不可逆类高牵挂拒绝自动兜底）。
- **技术债清零**：删 trade.go 死函数（消除 `go vet` unreachable）+ hunger 断粮致死走 Mutator（statuslint 3→1）；新增 ATB/combat_roll/记忆衰减确定性单测。
- **治理安全硬化**：内容安全双向审核接 3 chokepoint、4 个高危 ops 端点套 opsTokenGuard、真实实名 HTTPRealnameVerifier（不再信任客户端 bool、PII 不落库）。
- **商业化 + 大世界地基**：billing（SKU/购买/权益/真实 Apple+Google IAP 收据校验，未配网关安全拒绝）、region 分片接 runner 主循环（flag-gated 多实例租约）、regions/world_ticks 表 + 注册表、MySQL 连接池 8→64。
- AARRR 漏斗：**8 个事件已实际 Emit**（session_created/character_created/account_registered/purchase/intervention/decision_pending/inbox_opened/decision_resolved）。

---

## 四、已完成（端到端可用、接入主链路）

### 核心回合循环 / 战斗（含本会话）
- 两阶段状态机、ATB（gauge+势头惩罚）、三类指令+指令力、服从度五来源归因、异步执行+断线重连——端到端默认开启。
- **combat_shake 接主循环**：触发覆写决策 + Modifiers 合流；retreat 相邻格、rage 可见敌集、倍率落真实伤害（经 Mutator）。
- 确定性 combat_roll（FNV）、地形/天气/设施战斗修正、技能、战场生成；**本会话补 ATB/combat_roll/记忆衰减确定性单测**。

### 命运层与前台
- 三档路由 + FateScore（不可逆×牵挂×情绪）、待决策真后果（Mutator + 原子去重 + 归属校验）、**M2 每日≤3 + 48h 倒计时 + 过期兜底回响**、回响 Echo 接 live 决策链、WorldizeDeath 接战斗击杀。
- **命运四槽（FatePanel）+ 抗命溯源卡（DefianceCard）接主指挥客户端**；独立 `#fate` onboarding 仍在。

### 治理安全（本会话硬化）
- 内容安全双向审核（3 chokepoint）、隐私不可逆擦除、举报+审计打包、statuslint（1 历史违例）、**4 高危 ops 端点 opsTokenGuard**、**真实实名 HTTPRealnameVerifier**（身份证格式校验 + 匹配才置位 + PII 不落库/不入日志）。

### 商业化 / 大世界地基（本会话）
- billing：SKU 目录、Purchase（收据校验→权益+流水+存证，失败落 failed）、**真实 Apple verifyReceipt**（status==0/21007 sandbox 重试，缺 status 不放行）、**真实 Google Play 校验**、未配网关 denyVerifier 安全拒绝。
- **region 分片接 runner 主循环**（schedulePass 按区抢租、ClaimNextJobInRegion、区索引）、flag 默认关零行为变化；regions/world_ticks 表 + region 注册表（活跃度/threat/逻辑时钟）；连接池 64。

### 既有扎实项（未改动）
- AI-LLM 故障转移+schema 强校验+成本估算；四轴关系+恋爱+情报；饥饿/天气/生产/贸易/建筑全接主链路（**本会话清 trade 死码 + hunger 走 Mutator**）；结构化记忆+衰减+名人堂+信鸽+生育；WebSocket+Pixi 分层渲染+战争迷雾；存储双驱动（含真 MySQL 验证）+ state_json 拆分。

---

## 五、部分完成（有能力但未接主链路 / 默认 flag 关 / 半接入）

### 引擎原语「建好未接」（基本未变）
- **decision.Router 真短路默认关**（QUNXIANG_REFLEX_SHORTCIRCUIT），战斗主循环仍直调 LLM。
- **scheduler/encounter/attachment 全挂 region-runner**（默认关）；attachment 的 ReturnVisits 恒 0。
- **arbitration 唯一自治入口缺位**：MatchIntoSocialObject/boss 分赃仅 ops-HTTP，无自治玩法构造候选——反 P2W 机制保证尚无玩家可感知应用点。

### 命运层剩余
- ~~**命运卡倒计时前端不渲染**~~ → ✅ **已修复**：`FateCountdownBar` 每秒重渲标红/标灰（`FatePanel.tsx:176`/`FateView.tsx:271`，`countdown.ts:62`）。
- **主指挥客户端的局不种村**：SeedVillage 只从 `/api/units/bootstrap` 触发，主客户端 `createSinglePlayerSession` 路径不调——主玩法局没有 20 人关系网。
- M3 后果闸未泛化：fate-resolve 路径 daysAlive=0、D0-D3 硬锁只在 threat 遭遇链生效；§4.3 provenance 链未在 fate 后果路径强制。

### 治理安全剩余
- ~~**未成年宵禁/防沉迷未强制**：Gate 只是查询端点；RecordPlaySeconds 零调用。~~ → ✅ **已修复**（`79dc17f`）：`complianceGate()` 挂建局/advance-phase，`RecordPlaySeconds` 2 处累加。**剩余**：仅建局+advance-phase 挂了，`SetFactionDirective`（下指令）尚未挂 Gate；且整套受 `QUNXIANG_COMPLIANCE_ENABLED` 默认关。

### 跨玩家与 PvE（elite 已接，其余 ops-only）
- **单人 elite 遭遇全链路 + 前端入口已接主客户端**（done）。
- field_boss/world_boss/七种交互/consent 三档/社会客体撮合：**全部仅 ops-HTTP 可达、无玩家 UI、无自治驱动**（候选/队伍须外部手传）；region.threat_level 触发仍是硬编码常量。
- ~~跨玩家事件→收件箱桥接 `SurfaceCrossEventsForCharacter` **无 live 调用方**。~~ → ✅ **已修复**：`cross_link.go:29 surfaceCrossEventsAtBoundary`→`service.go:843`（部署边界，仅 WorldID 非空生效）。

### 商业化剩余
- Google IAP token 源是静态 env（非 service-account OAuth2 自动刷新，注释自陈生产勿长用）。
- ListEntitlements 无 HTTP 路由（玩家查不到已购权益）。

### 前端 / 实时
- WS `llm_interaction` 单独事件前端无分支（改从快照读）；WS 心跳 120s 读超时永不刷新→周期性短暂断连后自动重连兜底。

---

## 六、完全没做（仅文档 / 仅 stub / 死代码）

### ~~账户级成本闭环（三连缺，互为前提）~~ → ✅ 已修复（见顶部复核修订）
> 以下三条**旧结论已作废**：`79dc17f`/`68cbfd4` 后 session 已绑 `account_id`（`types.go:532`）、`billing.AddSpend` 24 调用点记账、`CheckQuota` 已接 LLM 前置拦截。整套受 `QUNXIANG_BILLING_ENABLED` 默认关（flag 而非缺代码）。
- ~~**session 未绑定 account_id**（types.go 无 AccountID）——账户级配额无从归集 LLM 成本的**根因**。~~
- ~~**chargeLLMQuota / billing.AddSpend 零调用方**（死代码）→ account_llm_quota.spent 永远为 0。~~（注：`chargeLLMQuota` 仍为死代码，但职责已被 `SpendRecorder` 取代）
- ~~**账户级配额闸未接 LLM 路径**：CheckQuota 是孤立查询端点，不拦截任何 LLM 调用。~~

### 引擎原语 / 战斗
- ~~**status.Mutator.ApplyBatch 零非测试调用方**（死代码）。~~ → ✅ **已修复**（`68cbfd4`）：经 `applyStatusMutationsBatch` 接入 8 处生产路径，`applyCombatGroupDynamics` 在战斗主循环每次击倒/暴击调用。
- reaction_queue 确定性决策解析器整套 Deprecated 死代码（仅提示词半边在用）。**【属实，待清理 ~400 行】**

### 大世界 / 跨玩家（设计承诺，原语在库未接线）
- 多实例真正分片的 **leader 选举 / region→实例稳定哈希 / rebalance**（当前只是「抢租即处理」对称争用切片）。**【仍真缺】** ~~region 注册表无任何包外调用方~~ → ✅ 已有调用方（`regionrunner.go`/`threat.go`/`ambient_scheduling.go`）。
- 关系图传播 + 二手消息失真 + propagation_log + blood_feud 衍生撮合（§1.4/§2.7）整块未接线。**【仍真缺——唯一真 missing】** 注：`68cbfd4` 只做了 blood_feud 直系单跳（hop=0），多跳传播 `blood_feud.go:234` 自承"留作后续接入"。
- regions PK 与 world_ticks 复合 PK 语义待对齐（region_id 全局唯一时不可触发，接线前需统一）。

### 命运 / 叙事产品化
- 可分享「死亡传记卡」（Spotify-Wrapped 竖图，变现/病毒核心物）；持久化编年史 chronicle_entries + 「回到那一刻」链接；角色创建「仇人/宿敌可追溯记忆种子」；翻译层 §4.1 模板；redline 离线宪章结构化数据源。

### ~~前端 UI 覆盖（后端能力悬空）~~ → ✅ 大部分已实现（见顶部复核修订）
> **旧结论严重过时**：治理 `GovernancePanel`、商业化 `BillingPanel`、实名 `ComplianceGate`、跨玩家同意 `ConsentInbox` 均已实现 + api wrapper 齐 + 挂载 `App.tsx`。
- ~~**治理 UI**（举报/审计/隐私擦除）：零玩家界面。~~ → ✅ `GovernancePanel.tsx`（`App.tsx:3658`）
- ~~**商业化/实名 UI**（内购/实名/配额）：前端连 API wrapper 都没有。~~ → ✅ `BillingPanel.tsx`（`App.tsx:2565`）+ `ComplianceGate.tsx`（`App.tsx:2552`）（后端默认 flag 关，玩家点购买当前无后端）
- **跨玩家 consent UI** → ✅ `ConsentInbox.tsx`（`App.tsx:3636`）；**world boss / 七交互 / ops 运营看板前端**【仍缺，属实】。

### 合规前置门（PRD 列为可致项目归零）
- ~~未成年模式实际强制 / 防沉迷计时实际累加——能力在库但**未挂在任何玩法端点**。~~ → ✅ **已修复**（`79dc17f`）：`complianceGate()` 挂 3 chokepoint、`RecordPlaySeconds` 累加。**但受 `QUNXIANG_COMPLIANCE_ENABLED` 默认关，关时端点整组 404、Gate 恒放行。**
- **年龄分级内容开关【仍真缺】**：`minor_mode` 仅 `c.Set` 写入 gin 上下文（`router.go:238`）**零读取方**，恋爱/生育/暴力内容无任何分级——出海/版号风险。

### 测试
- 前端测试「写了不跑」（仅 tsc 编译检查，无 runner 实际执行抗命卡解析断言）；combat_shake 主循环插桩缺端到端 DB/LLM 集成测试；region 注册表 MySQL 路径仅 SQLite 测试覆盖。
- 漏斗：5/7 P1 常量补全但无实际 Emit 点（留存/转介/合规拦截漏斗无数据）。

---

## 七、建议优先级（更新版剩余）

### P0 — 让已建能力对玩家真正生效
- 命运卡**倒计时前端渲染**（扩 FateCard 类型 + 渲染剩余小时）——M2② 收尾。
- **主指挥客户端建局路径接 SeedVillage**（主玩法局兑现 20 人关系网，否则命运层无锚可点亮）。
- **未成年/防沉迷 Gate 挂成前置中间件** + RecordPlaySeconds 接入（否则合规门=没门）。
- field/world boss + 七交互 + consent 给**玩家 UI 入口**（后端已就绪，纯前端 + api wrapper 缺口）。

### P1 — 闭环与可靠性
- **session↔account 绑定** → 接 chargeLLMQuota 进 LLM 成本结算 + CheckQuota 进 LLM 前置拦截（打通账户级成本封顶，三连缺的根因优先）。
- 接 **status.ApplyBatch** 进决策落地（兑现 DB 往返降本）。
- 治理/商业化**前端 UI**（举报/审计/隐私 + 内购/实名/配额）。
- 补 5 个未 Emit 的漏斗埋点 + 前端测试 runner + combat_shake 端到端测试。

### P2 — 自治驱动与规模化
- PvE/跨玩家/撮合的**自治驱动**（region.threat_level 累积触发、世界自然产出社会客体再择人），替代 ops-HTTP 手动。
- 关系图传播 + blood_feud 衍生撮合（§1.4/§2.7）。
- 多实例真正分片（leader 选举 / region 稳定哈希）；Google IAP service-account OAuth2；regions PK 对齐。

---

*依据：多智能体并行审计（每条附 file:line / 文档章节证据），8 个本会话改动子系统重新核验 + 5 个未改子系统沿用上轮。证据细节见对应代码与设计文档。*
