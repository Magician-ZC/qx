# 群像战棋 · 深度交叉验证审计：进度文档 vs 实际代码（2026-06-08）

> **方法**：25 个 Opus 子智能体，12 功能域并行回代码实证 + 对每域 `confirmed_done` 断言做对抗式反驳复核 + 工程门禁实跑（go build/test/vet/statuslint + 前端 build/lint）。
> **基线**：git `HEAD = 68cbfd4`。
> **判据**（本项目最关键的成熟度区别）：原语/能力已实现 **≠** 接入玩家可感知主链路；函数存在 **≠** 有非测试调用方；flag 默认关 **=** 默认部署玩家零感知。
> 每条结论附 `file:line` 实证。本报告为本次审计的**单一权威真相源**，盘点类文档（`项目家底盘点-完成与缺口.md` / `当前游戏完成度盘点.md` / `开发进度.md`）以此为准修订。

---

## 0. 一句话结论

**盘点文档显著滞后于代码**：commit `79dc17f`（三道门）与 `68cbfd4`（补四缺口）修复的大量"缺口"未刷新进盘点——本次核实出 **15 条"文档过度悲观"（缺口实际已修复）+ 1 条过时缺口**。项目真实瓶颈已从 **"没做"** 转移到 **"做了但 flag 默认关 → 默认部署玩家零感知"**。后端四道门全绿，前端 build 绿、lint 红（6 errors）。唯一真 `missing`：blood_feud 多跳关系图传播（只做了直系单跳 hop=0）。

---

## 1. 核实总计

86 条文档声称逐条回代码核实：

| verdict | 数量 | 含义 |
|---|---:|---|
| `confirmed_done` | 49 | 文档说完成，代码核实确实端到端接入主链路、有真实非测试调用方 |
| `partial` | 21 | 能力在但未接主链路 / flag 默认关 / 半接入 |
| `doc_understated` | 15 | **文档过度悲观**：缺口实际已被后续 commit 修复，文档需上修 |
| `doc_overclaim` | 1 | 文档把已修复的当缺口（"前端测试写了不跑"已不成立） |
| `missing` | 1 | 真缺口/死代码（blood_feud 多跳传播） |

**对抗复核**：51 条 `confirmed_done` 复核，仅 **governance-compliance 域降级 3 条**（揭示 flag 关时端点不注册/恒放行，详见 §6）；其余 11 域全部维持。

### 工程门禁实测（基线 68cbfd4）

| 命令 | 状态 | 关键事实 |
|---|---|---|
| `go build ./...` | ✅ pass | 全包编译通过 |
| `go test ./...` | ✅ pass | 全部带测试的包 ok，无失败用例 |
| `go vet ./...` | ✅ pass | **CLAUDE.md 提到的 `trade.go:68` unreachable 已不复现** |
| `go run ./cmd/statuslint ./...` | ⚠️ 1 违例 | 仅剩 `romance.go:1001`（新生儿 `child.Status.HP=60` 直赋）；CLAUDE.md 记载的 3 处（`hunger.go:176-177`、`romance.go:999`）已收敛 |
| 前端 `npm run build` | ✅ pass | tsc + vite 通过；`PixiBoard.js` 506KB >500KB 仅体积提示 |
| 前端 `npm run lint` | ❌ **fail** | **6 errors 阻断**：`App.tsx`（501 `showRoomSettings`、5642 `phaseHelpText`、6057 `_session` 未用）+ `bootstrapPixi.ts`（492/930 `no-explicit-any`、2212 `unitGlyph` 未用）；另 12 warnings |

> 注：CLAUDE.md 的「statuslint 3 处历史违例 / trade.go unreachable」描述也已过时，建议同步刷新。

---

## 2. ⚠️ 文档需上修：缺口实际已修复（15 条 doc_understated + 1 doc_overclaim）

这是本次审计**最重要的发现**——盘点文档的"缺口"区里，以下条目代码核实**已端到端落地**（多由 `79dc17f` / `68cbfd4` 修复），文档须从"缺口"移出。

### 2.1 账户级成本闭环「三连缺」全部已通（盘点曾列为"完全没做/死代码"）

| 旧声称（盘点 §六） | 实际 | 证据 |
|---|---|---|
| session 未绑 account_id（根因） | **已绑** | `types.go:532` `State.AccountID`（随 state_json 持久化）；`router.go:542-543/594-596` 经 `softAccountID` 从 token 软解析写入；`service.go:295-297,347` |
| `chargeLLMQuota`/`billing.AddSpend` 零调用方（死代码） | **AddSpend 已 24 调用点** | `service.go:151` `recordLLMSpendBestEffort`→`spend.AddSpend`，经 `appendLLMInteractionWithSpend` 在 pigeon/command/social/romance×6/combat_shake/intelligence/… 24 个非测试站点记账。（`session.chargeLLMQuota` 确为死代码，但职责已被注入的 `SpendRecorder=billing.Service` 取代，非功能缺口） |
| CheckQuota 孤立查询端点不拦截 LLM | **已接 LLM 前置拦截** | `service.go:166-188` `accountOverQuota`→`llmBlocked`，`llm.go` 7 处 GenerateJSON 站点（248/458/539/632/692/770/836）前置判定，命中走兜底 |
| ListEntitlements 无 HTTP 路由 | **已有路由 + 前端** | `service.go:524` + `GET /api/billing/entitlements/:accountId`（`router.go:1813`，token 取 accountID 防越权）；前端 `api.ts:851 fetchEntitlements` + `BillingPanel.tsx` 渲染 |

> ⚠️ 但**整套 billing 受 `QUNXIANG_BILLING_ENABLED` 默认关**：关时 `billingSvc=nil` → 4 路由不注册 + SpendRecorder 不注入 → 账户级记账/配额全链路 no-op。即"代码端到端就绪、线上零行为"。见 §3。

### 2.2 合规硬门槛已挂中间件 + 防沉迷计时累加（盘点曾列为"没门/未强制"）

| 旧声称 | 实际 | 证据 |
|---|---|---|
| Gate 只是查询端点、未作前置中间件 | **`complianceGate()` 已挂 3 chokepoint** | `router.go:211` 定义；挂在建局 `509`(single)/`560`(duel) + 推进 `1128`(advance-phase)；!Allowed→403+`compliance_blocked` 埋点（加入于 `79dc17f`） |
| RecordPlaySeconds 零调用→计时永不累加 | **已 2 处调用** | advance-phase 成功后 `router.go:1160` best-effort 累计 + 显式端点 `/api/compliance/play-seconds`（`router.go:1898`），加入于 `79dc17f` |

> ⚠️ 但**整套 compliance 受 `QUNXIANG_COMPLIANCE_ENABLED` 默认关**，且关时**端点整组根本不注册（返回 404）**、Gate 恒放行、RecordPlaySeconds no-op。见 §3 / §6。

### 2.3 前端 UI「零界面」已全部实现并挂载主客户端（盘点严重过时）

盘点 §六称"治理/商业化/跨玩家 UI 全缺、连 api wrapper 都没有"——**核实全部已实现**：

| UI | 组件 + 接线 | 挂载 |
|---|---|---|
| 治理（举报/审计/隐私擦除） | `GovernancePanel.tsx`→`submitModerationReport`/`getAuditBundle`/`eraseSessionPrivateData`(`api.ts:627/648/658`) 接 `router.go:988/1056/1109` | `App.tsx:3658` |
| 商业化（内购/配额/权益） | `BillingPanel.tsx`→`listBillingSKUs`/`purchaseSKU`/`getBillingQuota`/`fetchEntitlements` | `App.tsx:2565` |
| 实名/合规 | `ComplianceGate.tsx`→`verifyCompliance`/`getComplianceGate`/`reportPlaySeconds`(`api.ts:862/884/893`) | `App.tsx:2552` |
| 跨玩家同意 | `ConsentInbox.tsx`→`listPendingConsents`/`resolveConsent`(`api.ts:778/788`) 接 `router.go:402/410` | `App.tsx:3636` |

> 唯一仍属实的 UI 缺口：**ops/运营看板前端**（`/api/ops/leads-funnel`、cost-dashboard 仅后端 token 端点，前端 0 命中）。

### 2.4 其余已修复条目

| 旧声称 | 实际 | 证据 |
|---|---|---|
| 命运卡倒计时前端不渲染 | **已渲染** | `api.ts:61-68` FateCard 含 `expires_at?/countdown_hours?`；`countdown.ts:62-93` 逐秒递减；`FatePanel.tsx:176-193`+`FateView.tsx:271-283` `FateCountdownBar` 每秒重渲标红/标灰 |
| `status.Mutator.ApplyBatch` 死代码 | **接入 8 处生产路径**（`68cbfd4`） | 经 `applyStatusMutationsBatch`；`applyCombatGroupDynamics`(`service.go:3147/3188/3223/3262`) 由战斗伤害结算 `service.go:2803/2819` 在主循环每次击倒/暴击调用，无 flag |
| region 注册表无包外调用方 | **已有** | `regionrunner.go:359 UpsertRegion`/`363/369 SetActivityTier`/`398 ListByTier`/`426 AdvanceRegionTick`；`threat.go:97 BumpThreatLevel`；`ambient_scheduling.go:72/85` |
| region_leases 未接 runner 主循环 | **已接** | `schedulePass`(`regionrunner.go:295 AcquireLease`/`341 续租`)、`claimNextJobScoped:472` 持租区认领、`releaseHeldRegions:737` 退出释租，均织入主循环 |
| SurfaceCrossEventsForCharacter 无 live 调用方 | **已接部署边界** | `cross_link.go:29 surfaceCrossEventsAtBoundary`→`service.go:843`（Execution→Deployment 边界，仅 WorldID 非空时生效） |
| 漏斗 5/7 无 Emit | **12/13 有真实 Emit** | 13 个 analytics 常量中 12 个有 product_events Emit；唯 `return_visit` 刻意改走 `/api/leads`（防 1s/5s 轮询灌爆，`router.go:765-767` 注释说明）；`compliance_blocked` 有 Emit(`router.go:228`) |
| statuslint 仍剩 3 处历史违例 | **已 3→1** | hunger.go 两处已消除，仅剩 `romance.go:1001` |
| **前端测试"写了不跑"**（`doc_overclaim`） | **真跑真过** | `package.json` `"test":"vitest run"`；`npx vitest run` → 2 文件 20 用例全过，含 `fatePanel.defiance.test.ts` 12 个真实断言 + `countdown.test.ts` 8 个 |

---

## 3. 当前真实瓶颈：flag 默认关清单（代码就绪、默认部署玩家零感知）

这是项目当前的**核心成熟度真相**——大量子系统代码端到端真实、有非测试调用方，但被 env flag 默认关，默认部署下玩家/运营**完全感知不到**。要让它们"对玩家生效"，多数是**部署/产品决策**而非代码工作。

| flag（默认关） | 子系统 | 关时行为 |
|---|---|---|
| `QUNXIANG_BILLING_ENABLED` | 商业化全链路 | billingSvc=nil → 4 路由不注册、SpendRecorder 不注入、账户记账/配额 no-op |
| `QUNXIANG_COMPLIANCE_ENABLED` | 合规全链路 | **3 端点整组不注册（404）**、Gate 恒放行、RecordPlaySeconds no-op |
| `QUNXIANG_CONTENT_SAFETY` | 内容安全双向审核 | 3 chokepoint **恒放行**（`content_safety.go:141-143` 无条件 `Allowed:true`），不拦任何违规文本 |
| `QUNXIANG_MAIN_VILLAGE` | 主战局 20 人关系网 | `seedVillageForSession` no-op（`village.go:104-110`）→ 主玩法局命运层无锚可点 |
| `QUNXIANG_BLOOD_FEUD` | 血仇关系 | `blood_feud.go:45` 默认关 |
| `QUNXIANG_REFLEX_SHORTCIRCUIT` | decision.Router 真短路 | 仅影子遥测，主循环 100% LLM 直调（§7「<2% 上 LLM」未生效） |
| `QUNXIANG_PROMPT_CACHE` | prompt 缓存 | `prompt_cache.go` 默认 return nil；即便开也仅覆盖 unit decision 单点（`Cacheable` grep 仅 `llm.go:277`） |
| `QUNXIANG_REGION_RUNNER_*`（多个）+ `REGION_LEASES` | 离线大世界 runner（scheduler/encounter/attachment/HOT-LLM） | runner 不启动，冷热分层/锚密度威胁/离线自治线上不活 |
| `QUNXIANG_IAP_REAL` | 真实收据校验 | stub 向后兼容；**⚠️ 见 §5 刷单风险** |

---

## 4. 真实剩余开发任务清单

合计 58 项（P0×3 / P1×21 / P2×34）。下列 P0/P1 全列，P2 归类。

### P0 — 让命运开盒核心承诺对主玩法局玩家生效（3 项）

1. **主玩法局默认织 20 人关系网**：前端 `createSinglePlayerSession` 带 `with_village`（或后端建局默认织村），并把 `QUNXIANG_MAIN_VILLAGE` 转默认开。卡点：`seedVillageForSession` 受 flag 默认关 no-op、`api.ts:195` 不传 `with_village` → 主战局玩家感知不到"出生即 20 人关系网"，命运层无锚可点。〔fate-frontend〕
2. **离线宪章 `offline_charter`**：`Directive` 加 `Scope` 字段 + 长期目标/红线禁令/社交授权三段。这是 redline 接归因、Freeze List、reassessGoal 的**共同前置地基**（当前 `types.go` `Directive` 无 `Scope`）。〔social-memory〕
3. **redline 红线接入归因与相关性**：填充 `snap.Redlines`（`attribution_bridge.go:151` 当前恒空）+ `buildRelevanceAnchors` 写 Redline 锚（`fate.go:77`），让宪法 §4.1 红线 0.28 权重 + §5.1 redline cause 生效。engine 侧常量已就绪，纯缺生产写入；**依赖 P0-2**。〔social-memory〕

### P1 — 闭环、可靠性、乐趣钩子（21 项）

**命运前台乐趣钩子（留存核心）**
- 情境化待决策 Copilot：把固定三键（由她去/疾呼拦住/默默看着）按事件类型/红线/关系生成贴合情境的选项；需先扩后端 `ResolveFateDecision`（当前只认 3 类型）再扩前端。
- 主客户端 FatePanel 补高光卡分享/轻反馈（对齐 `#fate` FateView 的剪贴板 + `share_initiated` 埋点）；`FatePanel.tsx` 当前无 `onShare`。
- 可分享死亡/退役传记卡：`WorldizeDeath`/退役时生成图文分享卡（病毒传播北极星）；后端 legacy_hall/hall_of_fame 数据已有，前端未消费。
- 为命运卡填充真实归因因果句：写入 `ev.AttributionZH`（来自 `decision.Attribution.NarrativeZH`）；**渲染管线已就绪（`fate.go:55`+`916`）但无生产写入方，字段恒空 = 半接入死管线**。

**命运后果一致性**
- fate-resolve 牵挂计算把 `daysAlive` 从硬编码 0 改真实在世天数（`attachmentForUnit` `fate.go:548`），并在 fate 后果路径接 `encounter.PenaltyCap/DegradePenalty` 真 D0-D3 分级闸（当前仅 `threat.go:614` 调）。

**降本对默认部署生效**
- decision.Router 真短路从默认关扶正（评估 `/healthz` reflex `could_skip` 率后灰度），落地 §7「<2% 上 LLM」。
- prompt cache `Cacheable:true` 扩到对白/叙事/反思/交易高频站点，并灰度默认开。
- 把反射短路 + 账户级配额在影子统计验证后转默认开/灰度。

**自治驱动（反 P2W 机制对玩家生效）**
- boss 分赃/auto_match 自治撮合补线上默认可达触发器（如 `decision.Router` StrategicFork），让 `arbitration` 胜率∝Score/付费不进对玩家真实生效（当前仅 ops-HTTP 或 flag 关）。
- 默认开/灰度村庄关系网 + blood_feud。

**未成年模式语义落地（出海/版号闸门）**
- 把 Gate 裁出的 MinorMode 透传进 `session.Service`（`SetMinorMode`），在 romance/生育/combat 高暴力路径据此关闭恋爱·生育、降露骨暴力。**当前 `minor_mode` 仅 `c.Set` 写入 gin 上下文（`router.go:238`）零读取方**——年龄分级实际未生效。
- 出海/版号环境置 `QUNXIANG_COMPLIANCE_ENABLED=on` + 配真实实名网关。

**商业化上线决策**
- 决定 `QUNXIANG_BILLING_ENABLED` 是否默认开/灰度（关则 SKU/购买/配额/面板对玩家不可感知）。
- **⚠️ 生产开放购买前必须同时置 `QUNXIANG_IAP_REAL=1` + 配真实凭据**（见 §5）。

**大世界多实例 + 并发安全**
- 实现 leader 选举或 region→实例稳定哈希 + rebalance + 独立租约心跳（替换对称抢租，避免惊群/热点易主）。
- 开 `QUNXIANG_REGION_RUNNER_APPLY` 前先落地单位级串行化/乐观并发覆盖 `Mutator.Apply` 全量 status_json 写丢更新风险。

**跨玩家玩家可达**
- world boss 前端 UI（投放/列出/出手）接主客户端（后端机制完整、仅 ops/脚本可达）。
- 七交互（结识/结盟/联姻/反目/复仇/开战）加玩家可达入口 + 自治触发（`RecordSevenInteraction` 唯一调用方是 ops-guard `router.go:393`）。

**可靠性（实时通道 2 bug）**
- 前端 WS `onmessage` 加 `llm_interaction` case（`router.go:138` 推送当前被静默丢弃，需等下个全量 snapshot）。
- 修 WS 读超时刷新：`hub.go:144-170` 读循环对 app-ping 不刷新 `SetReadDeadline` → 安静连接约每 120s 强断重连 churn。

**测试**
- 为 combat_shake 主循环插桩补端到端 DB/LLM 集成测试（当前仅纯逻辑契约测试，主循环真实插桩无集成测试守护）。
- 为 batch 身份叙事接真实调用方（`narrative.go:116` 无调用点 → batch.go 去重并发能力无业务消费、属死代码）。

### P2 — 整洁度 / 增强 / 内容工业化（34 项，归类）

- **死代码清理**：reaction_queue ~400 行 Deprecated（`resolveReactionQueueDecision` 及 7 子函数）；`session.chargeLLMQuota`+配套；`maybeTriggerActionGapInteraction`(`social.go:20`)。
- **statuslint 清零**：`romance.go:1001` 新生儿初始化走白名单路径或专用 reason code。
- **角色长期动机**：reassessGoal 周期目标重估 + 六维野心向量；人格漂移调节器（`ReasonPersonalityDrift`）；村庄仇人/宿敌写入可查询结构化记忆（当前 SeedVillage 只播种四轴关系+锚）。
- **叙事产品化**：编年史物化视图 `chronicle_entries` + 回到那一刻链接；会话收尾卡。
- **引擎**：`attachment.ReturnVisits` 接真实回访计数（`attachment_compute.go:28` 硬编码 0）；fate 后果路径接 §4.3 provenance 强制；region-runner 全家桶灰度可感知。
- **AI**：分 tier 路由（按付费档/任务重要度差异化 provider/模型/超时）+ 前缀缓存（当前全缺）；`content_safety.extendedModeration` 从 no-op 升级真实 LLM/第三方审核。
- **跨玩家**：blood_feud 多跳传播 + 二手失真 + `propagation_log`（**唯一真 missing**，`blood_feud.go:234` 自承"留作后续接入"）；威胁刷新读回 `region.threat_level` 累积值替换硬编码常量；黑吃黑/背叛受害者回应卡；撮合玩家可达入口。
- **大世界**：统一 regions 与 world_ticks 的 PK 语义；region 升 HOT 非 wake 生产触发的端到端集成测试。
- **运营/合规**：ops/运营看板前端（leads-funnel + cost dashboard）；`SetFactionDirective` 路由也挂 complianceGate；玩家自助举报改账户/会话角色鉴权（当前 opsTokenGuard 默认放行）；订阅续期/退款回写权益。
- **生存经济**：hunger.go 余 ~30 处逐次 mutation 评估批量收敛。

---

## 5. 高危风险点（优先处理）

1. **🔴 IAP stub 刷单**：`QUNXIANG_BILLING_ENABLED` 与 `QUNXIANG_IAP_REAL` 正交——billing 开但 IAP_REAL 关 → Purchase 走 `stubVerifier` **恒 `ok=true`，任意收据都发权益**。生产开放购买前必须同时置 `QUNXIANG_IAP_REAL=1` + Apple shared secret / Google SA JSON，否则可被刷单。
2. **🟠 未成年模式形同虚设**：`minor_mode` 写进 gin 上下文后**零读取方**，恋爱/生育/暴力内容无任何分级开关——仅靠宵禁+时长拦截不满足年龄分级要求（出海/版号风险）。
3. **🟠 命运卡归因死管线**：`ev.AttributionZH` 渲染管线就绪但无生产写入、字段恒空 → "为什么会这样"对玩家永不显示（命运开盒"意外但合理"卖点的可见性缺口）。
4. **🟡 WS 120s churn**：安静连接约每 120s 被读超时强断重连（`hub.go:144-170` app-ping 不刷新 deadline）。
5. **🟡 代码注释过时**：`region/registry.go:16-18` 包头注释仍称"regionrunner.go 尚未消费"（实际已消费），误导后续开发者。

---

## 6. 对抗复核降级记录（governance-compliance 域 3 条）

对 51 条 `confirmed_done` 做反驳复核，仅此域降级——揭示"接线真实但 flag 关时默认 no-op"：

1. **内容安全双向审核** `confirmed_done → partial`：3 chokepoint 真实接线（`command.go:76` 输入侧、`llm.go:343/500/510` 输出侧），但 `ModerateText` 受 `QUNXIANG_CONTENT_SAFETY` 默认关，关时 `content_safety.go:141-143` 无条件放行 → 默认链路零审核。"接入"成立、"默认拦截"不成立。
2. **compliance 端点全注册** `confirmed_done → partial`：三端点（`router.go:1833/1865/1883`）整组包在 `if envFlag("QUNXIANG_COMPLIANCE_ENABLED")`（`router.go:1830`）内，默认关时**根本不注册（404）**；Gate 恒放行、RecordPlaySeconds no-op。"全注册/完整可用"overclaim。
3. **年龄分级/未成年内容开关** 维持 `missing`：反向推翻"其实已实现"失败——`minor_mode` 唯一写入点 `router.go:238`，全仓零 `Get/MustGet`，session 无 `SetMinorMode`，romance/hunger/combat 无 minor 分支。

> fate-backend 域另有 2 条在复核中确认（非降级）：M3 后果闸 `daysAlive=0`+D0-D3 未泛化到 fate（partial）、§4.3 provenance 未在 fate 后果路径强制（missing）——即文档对这两条 gap 的标注本就准确。

---

## 7. 建议

- **文档**：本报告为准；同步刷新 `项目家底盘点-完成与缺口.md`（§五/六 移除 §2 的 15 条已修复项）、`当前游戏完成度盘点.md`（工程门禁表 + region/UI 过时项）、CLAUDE.md（vet/statuslint 描述）；`开发进度.md` 补 `79dc17f`/`68cbfd4` 两批日志。
- **产品决策**：当前最大杠杆不是"再写后端"，而是 **(a) 把已就绪的 flag 子系统按发行需要灰度放开**（含 IAP_REAL 安全前置）、**(b) 命运前台乐趣钩子（情境化 Copilot + 分享传记卡 + 归因因果句）补留存**、**(c) 离线宪章 offline_charter 解锁 redline/Freeze List 一条龙**。
- **工程门禁**：前端 lint 6 errors 应清零（纯未用变量/explicit-any，低风险）；statuslint 最后 1 处收尾。
