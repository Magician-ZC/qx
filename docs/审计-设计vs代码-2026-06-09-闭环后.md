# 群像战棋 · 设计文档 vs 代码 全面审计（2026-06-09 闭环后）

> **方法**：10 个 Opus 子智能体按 10 类设计域并行深审，逐功能模块回代码实证（grep 调用方计数区分测试 + 读主链路接入点 + 查 flag 默认值 + 确认 reason-code/表/字段真有生产写入），分类 done / partial / flag_gated / stub / missing，再综合去重。
> **基线**：git `HEAD = 5514c93`（已含 `审计-设计文档vs代码-2026-06-09.md` §8 七波 + §9 二次闭环全部工作合入 main）。
> **与前份审计的区别**：那两份记录「缺口 + 实施闭环」；本份是**闭环后的当前真实快照**——回答「现在还有哪些功能模块未完成或缺失」。

---

## 0. 一句话结论

**地基扎实、引擎齐备，默认部署的最大瓶颈是「默认关的 flag」而非「缺代码」**——核心闭环 19 项已端到端落地且默认可感知；但**商业化对账 / 数据分析读端 / PvE 自治评估 / 发行级安全门**四块存在真实代码缺口，须补齐方达发行标准。

去重后约 42 个模块：**真缺口（partial/stub/missing）≈26**、**flag_gated（代码就绪、开启=发行/灰度/法务决策）≈14**、**MVP 外明确划界 1**（leader 选举/rebalance）、**done 19**。优先级：P0×1 / P1×16 / P2×39（多为 P2/P3 细项）/ P3×14。

---

## 1. 🔴 P0 — 结构性总开关（一个 flag 决定整层是否存在）

### 1.1 RegionRunner 离线引擎整条被 `QUNXIANG_REGION_RUNNER_ENABLED` 锁死（默认 false）
- **现状**：这是「<2% 上 LLM / 海量空闲 tick 零 LLM / 活世界离线自治 / PvE 威胁扎堆 / region 分片」全部能力的唯一上游闸。关时 `Run()` 首行 return、不起任何 goroutine、`seedAmbientForUnits` 全程 no-op、`/healthz` 的 region_runner 块 `ticks=0`。
- **代码就绪度**：端到端齐备、三包测试全绿（regionrunner/region/agentqueue）。证据：`cmd/server/main.go:59`（envBool 无默认=false）、`regionrunner.go:658 Run() if !cfg.Enabled return`。
- **杠杆**：它是下游 4+ 个 P1 能力（APPLY/LLM/THREATS/SHARDING）的共同前置——单 flag 决定整个「大世界沙盘」在生产是否存在。完整真打需 ENABLED+APPLY+THREATS+THREATS_APPLY 四 flag 皆开。

---

## 2. ❗ P1 — 真缺口（缺代码 / 缺读端 / no-op，直接影响发行或核心体验）

> 下列把「flag_gated 但其内部夹真缺口」也列入（如计费组里的订阅对账完全缺代码）。纯 flag_gated（代码完整、仅默认关）见 §4。

**商业化对账（真缺代码）**
- **订阅续费 / 退款 / 对账 — missing**：全仓 grep `refund|renewal|对账|RTDN|DID_RENEW|server.notif` 零命中。`grantEntitlement(billing/service.go:473)` 仅首购按 period 一次性算 expires_at，无 Apple S2S/ASSN、无 Google RTDN webhook、无退款→撤销 entitlement 链、无平台流水对账。订阅售出即静态到期。
- **微信/支付宝收据网关 — missing**：`newRealPlatformVerifier(service.go:131)` 仅注册 apple/google；微信/支付宝完全无代码（Apple/Google 网关 receipt_*.go 是真实非 stub，但受 IAP_REAL 默认关 + 走 stubVerifier）。

**数据分析读端（真缺读侧）**
- **北极星 D2 收件箱处理率口径错 — partial**：`analytics_query.go:132 InboxProcessRate=DecisionResolved/DecisionPending` 是窗口内原始事件计数比，非设计的 `COUNT(DISTINCT user_id)` 去重人头 / D2 自然日窗 / 排除 expired_fallback。数字无法对照设计阈值（>70/50-70/<50）。
- **维度列 user_id 跨会话归因几近缺失 — partial**：Emit 支持 UserID 但仅 `EmitReturnVisit(router.go:1723)` 一处真传；注册/创角/决策事件均不带 → 漏斗退化为 DISTINCT session_id，user 去重漏斗与跨会话付费归因无数据。
- **留存分层矩阵（T1/T2/T3 × D1/D2/D7）— missing**：grep `attachment_score/T1/T2/T3/firstperson_recap` 零命中，事件名常量都未定义。北极星看板⑪⑫无后端支撑。
- **A/B 分桶仅客户端白名单事件 — flag_gated+partial**：`AssignBucket(ab.go:27)` 就绪，但唯一接入点 `router.go:1449` 受 `QUNXIANG_AB_EXPERIMENT` 默认空门控，且只在 `/api/analytics/client` 分桶固定 2 变体；server 侧不分桶 → 红线 A/B 转化分母无数据，卖点 A/B/C 三桶不可表达。

**跨玩家「知情→回应」闭环（离线侧断裂）**
- **consent_gate 层2/3 对离线 A 零知情 — missing**：CONTESTED/REQUIRES_CONSENT 只落 `consent_requests(pending)` 行，全文件无 `SurfaceFateEvent` 调用 → 不给离线 A 投待决策/回响卡；超时只静默 UPDATE status=expired，无「她的命没等到回应」回响卡；A 的同意走中文子串匹配宪章而非 decision+attribution 自治管线。证据：`consent_gate.go`。

**PvE 自治与留痕（缺评估层 + 死码）**
- **参与意愿评估 join_intent — missing**：grep `join_intent/evaluateParticipation/ancestorAdvice` 零命中，THREAT_JOIN_* 三码零写入。参战由 endpoint 显式传 unitIDs 或 regionrunner 固定概率决定，角色不按人格/护短/惧战自评迎战，玩家叮嘱无法作偏置入场（PvE威胁系统 §2 整层缺）。
- **28 个 PvE reason-code 仅约 5 个有生产写入 — partial**：战斗用通用 COMBAT_HIT/Down/EmotionTrauma 代写，分赃用 ECONOMY_LOOT 而非 ECONOMY_LOOT_ARBITRATED → 反 P2W 胜率审计对单人/组队分赃查不到行（假阴）；THREAT_EMERGED/DEFEATED/WIPE/GEAR_DAMAGED/COMBAT_MAIMED/FELL_IN_DEFEAT/LEGACY_BEQUEATHED 等 21 码死码。

**发行级安全门（缺代码）**
- **输入侧越狱/prompt-injection 检测 — missing**：`content_safety.go` 仅确定性中文词表（约 30 词），grep `injection/jailbreak/越狱` 零生产命中。PRD §9 明确要求的输入侧检测完全无代码。
- **AI 安全红队 · 发行前 gate — missing**：grep `red.team/红队/release.gate/adversarial` 零命中。越狱/有害内容批量对抗评估 + pass/fail gate 未建立（PRD §9 发行级缺口）。

**前端来源链路断裂（UI 缺）**
- **M1 离线宪章无前端编辑 UI — partial**：后端 charter PUT/GET/DELETE 完整且红线/长期目标/社交授权已喂三处锚，但前端 grep `long_term_goals|redlines|/charter` 零命中 → 玩家只能靠外部 REST 立约/改约，捏人阶段不产出宪章。红线的「玩家来源」链路在 UI 断裂。
- **待决策 Copilot 情境选项仅粗类静态模板 + 独立 #fate 未接 — partial**：`buildFateChoices(fate.go:1291)` 按 3 类生成模板，主客户端 FatePanel 消费；但 `FateView.tsx:238` 独立 #fate 仍硬编码三键。情境选项非 per-narrative/非 LLM 生成。

**发行级 flag（默认关=裸奔，须发行前开）**
- **计费整组 flag_gated**：`QUNXIANG_BILLING_ENABLED + QUNXIANG_IAP_REAL` 双关 → SKU 永不播种、`/api/billing/*` 整组 404、真实收据网关走 stubVerifier、账户级配额硬护栏 `CheckQuota` 恒放行、叙事密度 perk 注入恒空。
- **合规裁决门 flag_gated**：`QUNXIANG_COMPLIANCE_ENABLED` 默认关 → 实名/防沉迷/宵禁/未成年模式四门恒 Allowed、端点不注册、`MinorMode` 恒 false → 未成年对恋爱/生育/暴力三个代码级硬门永不触发。**可致项目归零的发行前置门，默认部署裸奔。**
- **ops_token 默认放行**：`ops_auth.go:33` 为空时 c.Next() → 默认部署隐私擦除/purge/含完整 LLM prompt 的审计包/举报裁决等 19 处高危端点零鉴权对外。
- **Freeze List 默认关**：`QUNXIANG_FREEZE_LIST=false` → 卖传家宝/触红线/越社交禁令的离线动作直接落地不进收件箱，且收件箱失去离线侧主要供料源。ToS「AI 永不让角色做绝对红线」的系统级硬保证缺位。

---

## 3. 🟡 P2 — 深水区半落 / stub（品类深度，不阻断发行但削弱体验）

**沙盘 / 大世界**
- events 作用域双写只写 NULL（`ScopeContext` 三列就绪、全库无生产 populate，world_id/region_id 恒 NULL、tick 恒 0）→ 按 region 查事件/离线 catch-up 归属不成立。
- 自治胆量曲线（§5.2）缺：`rejectProbability` 无离线时长项，「越久越自主但越保守」两曲线未落地。
- 六维野心进**实时**决策打分未接：`ambitionActionWeight(*Service)` 在线决策路径零调用方（仅离线 regionrunner 经导出版消费）+ `QUNXIANG_AMBITION_SCORING` 默认关 → 在线野心只是 prompt 文本。
- 分 tier 路由实为任务重要度分档而非账户级算力账户：无 per-character/user/region/world 逐级 BudgetGate、无 llm_budget_accounts/llm_charges 流水表，`QUNXIANG_TIER_ROUTING` 默认关。
- geo 锚（进新 region）+ legacy 锚（血脉/传家物）无生产写入（定义完整、调用方=0）。

**事件耦合 / 跨玩家**
- 翻译矩阵非 data-driven DB 表（硬编码 Go map 3×6）、非全 reason_code×anchor_kind 覆盖、force_pending 专属模板缺。
- cross_events 缺 7+ 列（prev_cross_event_id/consent_state/arbitration_key/interaction_type/...）+ 无 cross_event_echoes/relevance_links 表 → 交互细节塞 payload_json 无法索引仲裁；罗生门 echo/共享历史证据链半落。
- 跨玩家零和裁决实为「会话内」：Key 用 sessionID 而非 worldID+SO.id，候选只取本会话 units，只覆盖联姻冲突一类（缺席位继承/排他战利品）。
- 七交互自治只发 4/7 类（缺 trade/marriage/war）+ 玩家发起入口 ops-only 不可达。
- 自动撮合 social_objects 默认关 + 表缺 region_id/severity/expires_at + 缺每日≤2 冷却 + 缺 NPC 兜底。
- M3 后果分级闸 layer2/layer3 精确门槛未逐条落地（连续近似而非「牵挂≥40 或在世≥3」「layer3 须已发生≥1 次 layer2」硬条件）。

**PvE 深水区**
- 组队/NPC backfill/黑吃黑（§7）整块缺失：ResolveFieldBoss 只是「传入 unitIDs 一次性多人结算」，无 party 社会客体/MatchScore 撮合/backfill_npc/黑吃黑 CROSS_BETRAYAL。
- Clutch→Legacy 传家物主动升级缺闭环（Contribution.Clutch 从不被战斗填充）。
- 失败旁观者 region_ravaged 死管线（`propagateRegionRavaged` 无生产调用方、REGION_RAVAGED 零写入）。
- world_boss 不从威胁度链升级（独立 FNV 凭空 spawn）+ 无 SoloAllowed 物理锁。
- 层3 不可逆 consent 降级 `defeatLayerAfterConsent(threat.go:798)` 是 stub（恒 return 2，无 RequiresConsent 卡/无超时降级残废）→ dungeon/world_boss 候选 layer3 接入后裸奔。
- 威胁刷新两路并存：部署边界固定 0.12 roll（默认开、无 threat_level 累积/锚加权选址）vs regionrunner 完整锚加权（锁在 THREATS 默认关）。
- 威胁触发未接战斗执行主循环（玩家在线聚焦战斗不会撞 elite，CLAUDE.md 自承后置）。

**商业化经济读端 / 内容**
- LTV·CAC·ARPPU·付费经济读端全缺（只有 purchase 计数，无金额 SUM/付费去重/cohort/回收期）。
- 天命点玩家可见数值资源是 stub（仅 {allowed:bool}，无 3000 天命/900 tick 余额、无耗尽分布埋点）。
- 内容母题库 40+ 建表零播种（season_content_themes 空表 + 可挂 NULL 指针骨架）。
- 内容安全 LLM/三方扩展审核层 no-op（`extendedModeration` 恒 return prior，规则词表外语义审核死占位）。

**治理 / 隐私 / 监控**
- 举报闭环鉴权语义错配（用 opsTokenGuard：未配 token 玩家可越权裁决他人、配了 token 玩家无法举报，无真正玩家级举报通道）。
- 保留期清理无自动调度（仅手动端点，无 cron）。
- 成本×留存联合监控口径不达设计（单 session 总成本而非单活跃角色日均，无 source 维度/ARPU 带/$0.18 黄$0.30 红阈值告警）。
- 假门卖点 A/B/C 漏斗按 source 拆分缺失。
- 漏斗末端事件埋点缺（chronicle_viewed/firstperson_recap_requested/direct_control_against_will 等常量未定义零 emit）。

**前端可玩 / 传播**
- 即时人格快照 15 秒微选择未实现（preview 仅静态传记卡，O2 最高 ROI 缺）。
- 可分享卡仅文本复制无图片卡（grep toBlob/html2canvas 零命中，不构成「愿意截图传播」的视觉物）。
- 新手首屏第一分钟缺显式引导（无 tutorial/tooltip 分步）。
- OOC 率双口径未交叉校准 + 玩家三键 OOC 无闭环回路（机器 OOC 自动降级有闭环，玩家主观 OOC 纯观测不驱动一致性收紧）。

---

## 4. 🚩 flag_gated 汇总（代码就绪 = 发行/灰度/法务决策，非缺代码，约 14 项）

| 组 | flag | 默认 | 开后生效 |
|---|---|---|---|
| **大世界引擎（总开关）** | `QUNXIANG_REGION_RUNNER_ENABLED`(+APPLY/LLM/THREATS/THREATS_APPLY/SHARDING/LEASES) | 关 | 离线自治真应用/HOT-LLM 决策/PvE 威胁遭遇/region 分片/租约（四 flag 全开才完整真打）|
| **商业化** | `QUNXIANG_BILLING_ENABLED`+`QUNXIANG_IAP_REAL` | 关 | SKU 目录/真实 Apple-Google 收据网关/账户级配额硬护栏/叙事密度 perk（内部仍夹订阅对账真缺口）|
| **合规法务** | `QUNXIANG_COMPLIANCE_ENABLED` / `QUNXIANG_CONTENT_SAFETY` / `QUNXIANG_OPS_TOKEN` | 关 | 实名宵禁防沉迷未成年门 / 双向内容审核 / 高危端点鉴权（**发行前必开**，内部夹越狱检测真缺口）|
| **自治增强（灰度）** | `QUNXIANG_FREEZE_LIST` / `SERENDIPITY` / `WORLDIZE_INBOUND` / `DUNGEON` / `AMBITION_SCORING` / `TIER_ROUTING` / `AB_EXPERIMENT` / `WORLD_BOSS_AUTO` / `AUTO_PVE` | 关 | 离线红线拦截/破圈/入向反查/副本/野心打分/分tier/A-B/世界Boss自刷/自动开打 |
| **部分默认开** | `AUTO_MATCH`/`AUTO_SOCIAL`/`WORLD_BINDING`(shared) | 部分开 | 社交自治/共享世界绑定 |

共同特征：代码端到端齐备、多数已接主链路拦截/触发点、有测试，默认关纯属保守发行/灰度/法务态，开 flag 即生效。

---

## 5. 不算缺口的澄清

- **多实例 leader 选举 / rebalance（missing）**：设计 §9 明确划界外的远期项；`region_leases` 已提供 per-region 互斥这一多实例核心安全保证，按设计意图非缺陷。
- **region 分片/租约（flag_gated）**：能力完整有 wire/race 测试，默认单实例退化为别名/恒抢到，单实例零行为变化。
- **events 三层归档 / 区域聚合器 / relevance_links 证据链表 / PvE 持久化五表 / 装备耐久衰减**：阶段3规模化深水区与审计/复放增强项（P3）。

---

## 6. ✅ 已端到端落地（19 项，默认可感知，仅标注）

零和仲裁确定性+付费不进 Score+事后审计 · 命运开盒 FateScore 三档+四槽前端 · 归因校验+OOC 遥测+自动降级+OOC_REJECTED 审计 · 牵挂四维(回访不为0)+收件箱预算/48h 兜底 · 回响 Echo order_echo · 命运卡 AttributionZH 当次 LLM 因果句 · M1 翻译矩阵接主路径 · 野心派生+人格漂移+目标重估接回合边界 · engine/encounter 结算原语 · 单人 elite 全链路+DB 集成测试 · 七交互模板+CrossIntent 协议 · AARRR 写读闭环+3 ops 端点 · 状态变更经 Mutator+statuslint 零违例 · IAP 纵深防伪 · 决策层 reflex 短路默认开+遥测 · 赛季 CRUD 骨架 · Freeze List 拦截点(仅 flag 关) · WorldizeDeath 接实战死亡 · region-runner 三包测试全绿+稳定哈希分片。

---

## 7. 建议下一步（按杠杆/成本比）

1. **发行前必做（P0/P1 安全门）**：开 `COMPLIANCE_ENABLED`+`CONTENT_SAFETY`+`OPS_TOKEN` 并补**输入侧越狱检测**与**发行前红队 gate**（缺代码）；订阅退款对账（缺代码）。
2. **通电高杠杆（P0）**：灰度开 `REGION_RUNNER_ENABLED` 全家桶 → 点亮「<2% 上 LLM / 离线自治 / PvE 威胁扎堆」整层（决定大世界在生产是否存在）。
3. **数据可观测（P1）**：补北极星 D2 去重口径 + user_id 全埋点 + 留存分层矩阵 + LTV/CAC 读端（缺读侧，决定能否做增长决策）。
4. **PvE 自治补完（P1/P2）**：join_intent 参与意愿评估 + PvE 专属 reason-code 真写入（修审计假阴）+ 层3 consent 降级（去 stub）。
5. **前端来源链路（P1）**：宪章编辑 UI + 待决策 Copilot 情境选项 + 即时人格快照微选择（决定留存乐趣）。

---

## 8. 游戏功能模块闭环（2026-06-09 续，5 波多 agent，已排除商业化/发行/合规/安全）

> 承本审计 §2-§3，对**游戏功能模块**缺口做 5 波多 agent 编排闭环（**明确排除商业化/订阅/支付、发行/合规/实名防沉迷、内容安全/越狱/红队、ops 鉴权**——这些是发行/法务决策非游戏机制）。每波：spine 先行 + 互斥文件 agent + 主控集成 + 六门禁 + 独立 commit；末波全波次对抗评审（17 发现→10 确证→全修）。提交链 1cf318f→429fe2a 已 ff 合入 main，六门禁全绿。

### Wave1 PvE 自治与留痕（1cf318f）
join_intent 参与意愿评估（反射护栏+意愿评分+归因 ValidateAttribution 无源不参战+祖魂引导偏置）· PvE 专属 reason-code 真写入（修零和审计假阴：排他件 ECONOMY_LOOT_ARBITRATED 带 Scope + THREAT_EMERGED/DEFEATED/WIPE/ALLY_DOWN + 败北分级码）· 层3 consent 去 stub（RequiresConsent 卡 + 超时降级 COMBAT_MAIMED 残废绝不阵亡）· region_ravaged 旁观传播接通 · world_boss SoloAllowed 物理锁 + 威胁链升级 provenance · Clutch 进 Score + 装备耐久 GEAR_DAMAGED + Clutch→Legacy 传家物升级闭环（+GateSurprise 永久锚 Reject）· 威胁刷新真调度（threat_level 累积+锚加权选址）。

### Wave2 跨玩家/事件耦合（d7f9e5d）
consent 层2/3 离线 A 知情（投待决策/高光卡）+ 自治回应过 decision+attribution + 超时回响 · 零和裁决真跨会话（Key=worldID+SO.id+tick + 跨 session 候选只读 + 三类 contest）· 七交互补全 trade/marriage/war 自治触发 · 罗生门 echo（cross_event_echoes 视角层事实唯一）+ 黑吃黑受害者卡 + blood_feud 社会客体 + 衍生传播 · 撮合升级（region 就近/severity/expires/每日≤2 冷却/NPC 兜底）· geo/legacy 锚首次生产写入。spine：13 cross 码 + cross_events 补 10 列 + social_objects 补 3 列 + cross_event_echoes 表。

### Wave3 命运/沙盘机制（80c0152）
M3 后果分级闸逐条门槛（层2 care≥40 或在世≥3；层3 四条 AND）· OOC 双口径闭环（玩家三键 OOC>15% latch 收紧 GateSurprise/破圈）· M1 翻译矩阵 data-driven DB 表（translation_templates 全覆盖 9 码×6 锚 + force_pending 专属 + 缺模板遥测）· 自治胆量曲线（离线越久越保守，确定性回合差）· 六维野心进在线决策（OnlineAmbitionActionWeight 接 fallback 择优+服从权衡）· events 作用域双写 populate · 编年史读侧接通（端点+前端 ChroniclePanel）。spine：translation_templates 表。

### Wave4 前端可玩体验（170bd75）
待决策 Copilot 情境选项（独立 #fate 消费后端 buildFateChoices）· 即时人格快照 15 秒微选择（persona 八轴确定性拣题+人格速写）· 可分享卡图片化（无依赖 canvas 死亡/高光/传记卡+复制图片/下载）· 离线宪章前端编辑 UI（CharterEditor 红线/长期目标/社交授权增删改）· 新手首屏第一分钟引导（OnboardingTour 祖魂心智模型，localStorage 判重）。

### Wave5 对抗评审 + 修复（429fe2a）
17 发现→10 确证全修：**2 high 跨玩家硬不变量违反**（consent 边界全局无作用域替别局自治接受直写 B 的 relations；contest 补偿直写他人 session 单位记忆 blob）· 2 medium（world_boss 单人锁被绕过；跨会话裁决无单一裁决者）· 6 low（elite Clutch 死代码/黑吃黑冷却/胆量曲线 time.Now 确定性/healthz 性能/FateView 去重等）。

### 本轮闭环后仍延后（非游戏机制 / 设计 MVP 外 / 独立大工程）
- **商业化/发行/合规/安全**（本轮明确排除）：订阅对账/天命点/LTV-CAC、实名防沉迷、内容安全/越狱检测/红队、ops 鉴权——发行/法务决策。
- **设计 MVP 外**：多实例 leader 选举/rebalance/region 真分片（对称抢租已足）。
- **独立大工程**：PvE 组队 party/NPC backfill 完整撮合（黑吃黑/罗生门已落）；burn-in 活史/赛季内容母题播种；副本离线 tick 自动推进（HTTP 逐段+charter 超时已落）；events 三层归档/区域聚合器（规模化阶段3）。
- **跨玩家单一裁决者残留**：缺 world-runner 下零和裁决已做到「确定性同胜者+幂等」，完整中心化裁决待 world-runner。
- **flag 默认值**：游戏性 flag（dungeon/worldize_inbound/serendipity/ambition_scoring/auto_pve/world_boss_auto/courage_curve/consistency_tighten 等）代码就绪、默认关待灰度——开启是玩法灰度决策（非缺代码）。
